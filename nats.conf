jetstream {
    store_dir: "/data"
}

http_port: 8222

websocket {
    port: 8080
    no_tls: true
}

accounts {
    APP {
        jetstream: enabled
        users: [
            { user: $NATS_AUTH_USER, password: $NATS_AUTH_PASSWORD }
            { user: $NATS_SERVER_USER, password: $NATS_SERVER_PASSWORD }
        ]
    }
}

authorization {
    auth_callout {
        issuer: $NATS_AUTH_ISSUER
        auth_users: [ $NATS_AUTH_USER, $NATS_SERVER_USER ]
        account: APP
    }
}
