Система: {{ message.meta.os }}
Версия приложения: {{ message.meta.appVersion }}
Email: {% if message.emailFrom %}{{ message.emailFrom | safe }}{% else %}не указан{% endif %}
Информация о резидентах:
{%- if message.meta.residentsExtraInfo.length == 0 -%}
  нет
{%- else -%}
 {% for resident in message.meta.residentsExtraInfo %}
    Адрес: {% if resident.address %}{{ resident.address | safe }}{% else %}не указан{% endif %}.
    Лицевой счёт: {% if resident.accountNumber %}{{ resident.accountNumber | safe }}{% else %}не указан{% endif %}.
    Категория платежей: {% if resident.paymentCategory %}{{ resident.paymentCategory | safe }}{% else %}не указана{% endif %}.

 {% endfor %}
{% endif %}
Сообщение: {{ message.meta.text | safe }}
УК:
{%- if message.meta.organizationsData.length == 0 -%}
  нет
{%- else -%}
  {% for org in message.meta.organizationsData %}
    - {{ org.name }}. ИНН: {{ org.tin }}
  {% endfor %}
{% endif %}
