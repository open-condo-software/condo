/**
 * Generated by `createschema organization.OrganizationEmployeeSpecialization 'employee:Relationship:OrganizationEmployee:CASCADE; specialization:Relationship:TicketCategoryClassifier:CASCADE;'`
 */

const { historical, versioned, uuided, tracked, softDeleted, dvAndSender, analytical } = require('@open-condo/keystone/plugins')
const { GQLListSchema } = require('@open-condo/keystone/schema')

const access = require('@condo/domains/organization/access/OrganizationEmployeeSpecialization')


const OrganizationEmployeeSpecialization = new GQLListSchema('OrganizationEmployeeSpecialization', {
    schemaDoc: 'A work category, that employee can perform. Used to limit the visibility of tickets for employees with "propertyAndSpecialization" ticketVisibilityType',
    fields: {

        employee: {
            type: 'Relationship',
            ref: 'OrganizationEmployee',
            isRequired: true,
            knexOptions: { isNotNullable: true },
            kmigratorOptions: { null: false, on_delete: 'models.CASCADE' },
        },

        specialization: {
            schemaDoc: 'Specific work category, that this employee can perform',
            type: 'Relationship',
            ref: 'TicketCategoryClassifier',
            isRequired: true,
            knexOptions: { isNotNullable: true },
            kmigratorOptions: { null: false, on_delete: 'models.CASCADE' },
        },

    },
    kmigratorOptions: {
        constraints: [
            {
                type: 'models.UniqueConstraint',
                fields: ['employee', 'specialization'],
                condition: 'Q(deletedAt__isnull=True)',
                name: 'organization_employee_specialization_unique_employee_and_specialization',
            },
        ],
    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), dvAndSender(), historical(), analytical()],
    access: {
        read: access.canReadOrganizationEmployeeSpecializations,
        create: access.canManageOrganizationEmployeeSpecializations,
        update: access.canManageOrganizationEmployeeSpecializations,
        delete: false,
        auth: true,
    },
})

module.exports = {
    OrganizationEmployeeSpecialization,
}
