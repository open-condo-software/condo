/**
 * Generated by `createschema organization.TokenAccessRight 'name:Text; user:Relationship:User:CASCADE;'`
 */

const { Select } = require('@keystonejs/fields')

const { historical, versioned, uuided, tracked, softDeleted, dvAndSender } = require('@open-condo/keystone/plugins')
const { GQLListSchema } = require('@open-condo/keystone/schema')

const { SERVICE_USER_FIELD } = require('@condo/domains/miniapp/schema/fields/accessRight')
const access = require('@condo/domains/organization/access/TokenAccessRight')

const { INTEGRATIONS_NAME, SBBOL_INTEGRATION_NAME } = require('../constants')


const TokenAccessRight = new GQLListSchema('TokenAccessRight', {
    schemaDoc: 'The scheme regulates the access of the service user to the access tokens stored in the redis',
    fields: {

        name: {
            schemaDoc: 'Which service token the user has access to',
            type: Select,
            dataType: 'enum',
            options: INTEGRATIONS_NAME,
            defaultValue: SBBOL_INTEGRATION_NAME,
            isRequired: true,
        },

        user: SERVICE_USER_FIELD,

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), dvAndSender(), historical()],
    access: {
        read: access.canReadTokenAccessRights,
        create: access.canManageTokenAccessRights,
        update: access.canManageTokenAccessRights,
        delete: false,
        auth: true,
    },
})

module.exports = {
    TokenAccessRight,
}
