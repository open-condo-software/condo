/**
 * Generated by `createschema organization.EmployeeFiltersTemplate 'name:Text; employee:Relationship:OrganizationEmployee:CASCADE; schemaName:Text; filters:Json'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { EmployeeFiltersTemplate as EmployeeFiltersTemplateGQL } from '@condo/domains/organization/gql'
import { EmployeeFiltersTemplate, EmployeeFiltersTemplateUpdateInput, QueryAllEmployeeFiltersTemplatesArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'name', 'employee', 'schemaName', 'filters']
const RELATIONS = ['employee']

export interface IEmployeeFiltersTemplateUIState extends EmployeeFiltersTemplate {
    id: string
    // TODO(codegen): write IEmployeeFiltersTemplateUIState or extends it from
}

function convertToUIState (item: EmployeeFiltersTemplate): IEmployeeFiltersTemplateUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IEmployeeFiltersTemplateUIState
}

export interface IEmployeeFiltersTemplateFormState {
    id?: string
    employee?: string
    name?: string
    schemaName?: string
    filters?: string
}

function convertToUIFormState (state: IEmployeeFiltersTemplateUIState): IEmployeeFiltersTemplateFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IEmployeeFiltersTemplateFormState
}

function convertToGQLInput (state: IEmployeeFiltersTemplateFormState): EmployeeFiltersTemplateUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<EmployeeFiltersTemplate, EmployeeFiltersTemplateUpdateInput, IEmployeeFiltersTemplateFormState, IEmployeeFiltersTemplateUIState, QueryAllEmployeeFiltersTemplatesArgs>(EmployeeFiltersTemplateGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
