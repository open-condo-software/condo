// @ts-nocheck
/**
 * Generated by `createschema organization.EmployeeFiltersTemplate 'name:Text; employee:Relationship:OrganizationEmployee:CASCADE; schemaName:Text; filters:Json'`
 */

const { throwAuthenticationError } = require("@condo/domains/common/utils/apolloErrorFormatter")

async function canReadEmployeeFiltersTemplates ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false
    if (user.isAdmin) return {}
    return {
        // TODO(codegen): write canReadEmployeeFiltersTemplates logic!
    }
}

async function canManageEmployeeFiltersTemplates ({ authentication: { item: user }, originalInput, operation, itemId }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false
    if (user.isAdmin) return true
    if (operation === 'create') {
        // TODO(codegen): write canManageEmployeeFiltersTemplates create logic!
        return true
    } else if (operation === 'update') {
        // TODO(codegen): write canManageEmployeeFiltersTemplates update logic!
        return true
    }
    return false
}

/*
  Rules are logical functions that used for list access, and may return a boolean (meaning
  all or no items are available) or a set of filters that limit the available items.
*/
module.exports = {
    canReadEmployeeFiltersTemplates,
    canManageEmployeeFiltersTemplates,
}
