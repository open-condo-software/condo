/**
 * Generated by `createschema ticket.TicketClassifierLink 'location?:Relationship:TicketClassifier:PROTECT;category?:Relationship:TicketClassifier:PROTECT;subject?:Relationship:TicketClassifier:PROTECT;'`
 */

async function canReadTicketClassifierLinks ({ authentication: { item: user } }) {
    if (!user) return false
    if (user.isAdmin) return {}
    return {
        // TODO(codegen): write canReadTicketClassifierLinks logic!
    }
}

async function canManageTicketClassifierLinks ({ authentication: { item: user }, originalInput, operation, itemId }) {
    if (!user) return false
    if (user.isAdmin) return true
    if (operation === 'create') {
        // TODO(codegen): write canManageTicketClassifierLinks create logic!
        return true
    } else if (operation === 'update') {
        // TODO(codegen): write canManageTicketClassifierLinks update logic!
        return true
    }
    return false
}

/*
  Rules are logical functions that used for list access, and may return a boolean (meaning
  all or no items are available) or a set of filters that limit the available items.
*/
module.exports = {
    canReadTicketClassifierLinks,
    canManageTicketClassifierLinks,
}
