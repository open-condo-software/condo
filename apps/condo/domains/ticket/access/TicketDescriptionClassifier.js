// @ts-nocheck
/**
 * Generated by `createschema ticket.TicketDescriptionClassifier 'organization?:Relationship:Organization:CASCADE;name:Text;'`
 */

const { throwAuthenticationError } = require("@condo/domains/common/utils/apolloErrorFormatter")

async function canReadTicketDescriptionClassifiers ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.isAdmin) return {}
    return {
        // TODO(codegen): write canReadTicketDescriptionClassifiers logic!
    }
}

async function canManageTicketDescriptionClassifiers ({ authentication: { item: user }, originalInput, operation, itemId }) {
    if (!user) return throwAuthenticationError()
    if (user.isAdmin) return true
    if (operation === 'create') {
        // TODO(codegen): write canManageTicketDescriptionClassifiers create logic!
        return true
    } else if (operation === 'update') {
        // TODO(codegen): write canManageTicketDescriptionClassifiers update logic!
        return true
    }
    return false
}

/*
Rules are logical functions that used for list access, and may return a boolean (meaning
all or no items are available) or a set of filters that limit the available items.
*/
module.exports = {
    canReadTicketDescriptionClassifiers,
    canManageTicketDescriptionClassifiers,
}
