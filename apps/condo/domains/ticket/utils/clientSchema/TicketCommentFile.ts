/**
 * Generated by `createschema ticket.TicketCommentFile 'organization:Relationship:Organization:CASCADE;file?:File;ticketComment?:Relationship:TicketComment:SET_NULL'`
 */

import { useCreateTicketCommentFileMutation, CreateTicketCommentFileMutation } from '@app/condo/gql'
import {
    TicketCommentFile,
    TicketCommentFileCreateInput,
    TicketCommentFileUpdateInput,
    QueryAllTicketCommentFilesArgs,
} from '@app/condo/schema'
import { UploadFile } from 'antd/lib/upload/interface'
import getConfig from 'next/config'

import { generateReactHooks } from '@open-condo/codegen/generate.hooks'
import { upload as uploadFiles, buildMeta } from '@open-condo/files'
import { getClientSideSenderInfo } from '@open-condo/miniapp-utils/helpers/sender'
import { useAuth } from '@open-condo/next/auth'
import { useOrganization } from '@open-condo/next/organization'

import { TicketCommentFile as TicketCommentFileGQL } from '@condo/domains/ticket/gql'

const {
    useObject,
    useObjects,
    useUpdate,
    useSoftDelete,
} = generateReactHooks<TicketCommentFile, TicketCommentFileCreateInput, TicketCommentFileUpdateInput, QueryAllTicketCommentFilesArgs>(TicketCommentFileGQL)

const { publicRuntimeConfig: { fileClientId, fileServerUrl } = {} } = getConfig()

type FileInputType = File | UploadFile | { signature: string, originalFilename: string, mimetype: string }
type CreatedTicketCommentFile = NonNullable<CreateTicketCommentFileMutation['ticketCommentFile']>

const useCreate = (attrs?: Partial<TicketCommentFileCreateInput>, onComplete?: (result?: CreatedTicketCommentFile) => Promise<void> | void) => {
    const [mutate] = useCreateTicketCommentFileMutation()
    const { user } = useAuth()
    const { organization } = useOrganization()

    return async (data: Partial<TicketCommentFileCreateInput>): Promise<CreatedTicketCommentFile | undefined> => {
        const sender = getClientSideSenderInfo()
        try {
            let fileField = data.file as FileInputType | undefined
            if (fileClientId && user?.id && fileField && !('signature' in fileField)) {
                const maybeFile = (fileField as UploadFile)?.originFileObj || fileField
                if (maybeFile && 'name' in maybeFile && 'type' in maybeFile) {
                    const uploadResult = await uploadFiles({
                        ...(fileServerUrl ? { serverUrl: fileServerUrl } : {}),
                        files: [maybeFile as File],
                        meta: buildMeta({
                            userId: user.id,
                            fileClientId,
                            modelNames: ['TicketCommentFile'],
                            fingerprint: sender.fingerprint,
                            organizationId: organization?.id,
                        }),
                    })

                    const signature = uploadResult.files?.[0]?.signature
                    if (signature) {
                        fileField = {
                            signature,
                            originalFilename: maybeFile.name,
                            mimetype: maybeFile.type,
                        }
                    }
                }
            }
            const variablesData = { dv: 1, sender, ...attrs, ...data, file: fileField }
            const res = await mutate({ variables: { data: variablesData } })
            const created = res?.data?.ticketCommentFile
            if (onComplete) await onComplete(created)
            return created
        } catch (err) {
            console.error('TicketCommentFile upload failed', err)
            throw err
        }
    }
}

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useSoftDelete,
}
