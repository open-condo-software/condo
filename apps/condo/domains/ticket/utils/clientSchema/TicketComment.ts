/**
 * Generated by `createschema ticket.TicketComment 'ticket:Relationship:Ticket:CASCADE; user:Relationship:User:CASCADE; content:Text;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { TicketComment as TicketCommentGQL } from '@condo/domains/ticket/gql'
import { TicketComment, TicketCommentUpdateInput, QueryAllTicketCommentsArgs, UserTypeType } from '@app/condo/schema'
import { ITicketCommentFileUIState } from './TicketCommentFile'

const FIELDS = ['id', 'deletedAt', 'type', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'ticket', 'user', 'content']
const RELATIONS = ['ticket', 'user']

export interface ITicketCommentUIState extends TicketComment {
    id: string,
    type: string,
    content: string,
    user: {
        id: string,
        name: string,
        type: UserTypeType,
    },
    files: ITicketCommentFileUIState[],
    createdAt: string,
    updatedAt: string,
    deletedAt: string,
}

function convertToUIState (item: TicketComment): ITicketCommentUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as ITicketCommentUIState
}

export interface ITicketCommentFormState {
    id?: undefined
    user?: string
    ticket?: string
    type?: string
}

function convertToUIFormState (state: ITicketCommentUIState): ITicketCommentFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as ITicketCommentFormState
}

function convertToGQLInput (state: ITicketCommentFormState): TicketCommentUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    useSoftDelete,
} = generateReactHooks<TicketComment, TicketCommentUpdateInput, ITicketCommentFormState, ITicketCommentUIState, QueryAllTicketCommentsArgs>(TicketCommentGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    useSoftDelete,
    convertToUIFormState,
}
