/**
 * Generated by `createschema ticket.TicketStatus 'organization?:Relationship:Organization:CASCADE; type:Select:neworreopened,processing,canceled,completed,deferred,closed; name:Text;' --force`
 */

import {
    TicketStatus,
    TicketStatusCreateInput,
    TicketStatusUpdateInput,
    QueryAllTicketStatusesArgs,
} from '@app/condo/schema'

import { generateReactHooks } from '@open-condo/codegen/generate.hooks'

import { TicketStatus as TicketStatusGQL } from '@condo/domains/ticket/gql'

export interface ITicketStatusFormSelectState {
    value: string
    label: string
    type: string
}

function convertGQLItemToFormSelectState (item: TicketStatus): ITicketStatusFormSelectState | undefined {
    if (!item) {
        return
    }

    const { name, id, type } = item

    return { value: id, label: name, type }
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useSoftDelete,
} = generateReactHooks<TicketStatus, TicketStatusCreateInput, TicketStatusUpdateInput, QueryAllTicketStatusesArgs>(TicketStatusGQL)

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useSoftDelete,
    convertGQLItemToFormSelectState,
}
