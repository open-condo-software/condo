/**
 * Generated by `createschema ticket.ExportTicketTask 'status:Select:processing,completed,error; format:Select:excel; exportedRecordsCount:Integer; totalRecordsCount:Integer; file?:File; meta?:Json'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { ExportTicketTask as ExportTicketTaskGQL } from '@condo/domains/ticket/gql'
import { ExportTicketTask, ExportTicketTaskUpdateInput, QueryAllExportTicketTasksArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'status', 'format', 'exportedRecordsCount', 'totalRecordsCount', 'file', 'meta']
const RELATIONS = []

export interface IExportTicketTaskUIState extends ExportTicketTask {
    id: string
    // TODO(codegen): write IExportTicketTaskUIState or extends it from
}

function convertToUIState (item: ExportTicketTask): IExportTicketTaskUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IExportTicketTaskUIState
}

export interface IExportTicketTaskFormState {
    id?: undefined
    // TODO(codegen): write IExportTicketTaskUIFormState or extends it from
}

function convertToUIFormState (state: IExportTicketTaskUIState): IExportTicketTaskFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IExportTicketTaskFormState
}

function convertToGQLInput (state: IExportTicketTaskFormState): ExportTicketTaskUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<ExportTicketTask, ExportTicketTaskUpdateInput, IExportTicketTaskFormState, IExportTicketTaskUIState, QueryAllExportTicketTasksArgs>(ExportTicketTaskGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
