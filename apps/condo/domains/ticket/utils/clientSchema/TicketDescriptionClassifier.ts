/**
 * Generated by `createschema ticket.TicketDescriptionClassifier 'organization?:Relationship:Organization:CASCADE;name:Text;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { TicketDescriptionClassifier as TicketDescriptionClassifierGQL } from '@condo/domains/ticket/gql'
import { TicketDescriptionClassifier, TicketDescriptionClassifierUpdateInput, QueryAllTicketDescriptionClassifiersArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'organization', 'name']
const RELATIONS = ['organization']

export interface ITicketDescriptionClassifierUIState extends TicketDescriptionClassifier {
    id: string
    // TODO(codegen): write ITicketDescriptionClassifierUIState or extends it from
}

function convertToUIState (item: TicketDescriptionClassifier): ITicketDescriptionClassifierUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as ITicketDescriptionClassifierUIState
}

export interface ITicketDescriptionClassifierFormState {
    id?: undefined
    // TODO(codegen): write ITicketDescriptionClassifierUIFormState or extends it from
}

function convertToUIFormState (state: ITicketDescriptionClassifierUIState): ITicketDescriptionClassifierFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as ITicketDescriptionClassifierFormState
}

function convertToGQLInput (state: ITicketDescriptionClassifierFormState): TicketDescriptionClassifierUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<TicketDescriptionClassifier, TicketDescriptionClassifierUpdateInput, ITicketDescriptionClassifierFormState, ITicketDescriptionClassifierUIState, QueryAllTicketDescriptionClassifiersArgs>(TicketDescriptionClassifierGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
