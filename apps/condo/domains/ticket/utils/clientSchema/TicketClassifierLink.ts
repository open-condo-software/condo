/**
 * Generated by `createschema ticket.TicketClassifierLink 'location?:Relationship:TicketClassifier:PROTECT;category?:Relationship:TicketClassifier:PROTECT;subject?:Relationship:TicketClassifier:PROTECT;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { TicketClassifierLink as TicketClassifierLinkGQL } from '@condo/domains/ticket/gql'
import { TicketClassifierLink, TicketClassifierLinkUpdateInput, QueryAllTicketClassifierLinksArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'location', 'category', 'subject']
const RELATIONS = ['location', 'category', 'subject']

export interface ITicketClassifierLinkUIState extends TicketClassifierLink {
    id: string
    // TODO(codegen): write ITicketClassifierLinkUIState or extends it from
}

function convertToUIState (item: TicketClassifierLink): ITicketClassifierLinkUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as ITicketClassifierLinkUIState
}

export interface ITicketClassifierLinkFormState {
    id?: undefined
    // TODO(codegen): write ITicketClassifierLinkUIFormState or extends it from
}

function convertToUIFormState (state: ITicketClassifierLinkUIState): ITicketClassifierLinkFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as ITicketClassifierLinkFormState
}

function convertToGQLInput (state: ITicketClassifierLinkFormState): TicketClassifierLinkUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<TicketClassifierLink, TicketClassifierLinkUpdateInput, ITicketClassifierLinkFormState, ITicketClassifierLinkUIState, QueryAllTicketClassifierLinksArgs>(TicketClassifierLinkGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
