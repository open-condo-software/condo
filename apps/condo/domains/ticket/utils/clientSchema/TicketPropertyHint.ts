/**
 * Generated by `createschema ticket.TicketPropertyHint 'organization:Relationship:Organization:CASCADE; name?:Text; properties:Relationship:Property:SET_NULL; content:Text;'`
 */

import { pick, get } from 'lodash'
import { TicketPropertyHint, TicketPropertyHintUpdateInput, QueryAllTicketPropertyHintsArgs } from '@app/condo/schema'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'
import { TicketPropertyHint as TicketPropertyHintGQL } from '@condo/domains/ticket/gql'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'organization', 'name', 'content']
const RELATIONS = ['organization']

export interface ITicketPropertyHintUIState extends TicketPropertyHint {
    id: string
}

function convertToUIState (item: TicketPropertyHint): ITicketPropertyHintUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as ITicketPropertyHintUIState
}

export interface ITicketPropertyHintFormState {
    id?: string
    organization?: string
}

function convertToUIFormState (state: ITicketPropertyHintUIState): ITicketPropertyHintFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as ITicketPropertyHintFormState
}

function convertToGQLInput (state: ITicketPropertyHintFormState): TicketPropertyHintUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    useSoftDelete,
} = generateReactHooks<TicketPropertyHint, TicketPropertyHintUpdateInput, ITicketPropertyHintFormState, ITicketPropertyHintUIState, QueryAllTicketPropertyHintsArgs>(TicketPropertyHintGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    useSoftDelete,
    convertToUIFormState,
}
