/**
 * Generated by `createschema ticket.TicketClassifierRule 'organization?:Relationship:Organization:CASCADE;place?:Relationship:TicketPlaceClassifier:PROTECT;category?:Relationship:TicketCategoryClassifier:PROTECT;description?:Relationship:TicketDescriptionClassifier:PROTECT;'`
 */

const { Text, Relationship, Integer, Select, Checkbox, DateTimeUtc, CalendarDay, Decimal, Password, File } = require('@keystonejs/fields')
const { Json } = require('@core/keystone/fields')
const { GQLListSchema } = require('@core/keystone/schema')
const { historical, versioned, uuided, tracked, softDeleted } = require('@core/keystone/plugins')
const { SENDER_FIELD, DV_FIELD } = require('@condo/domains/common/schema/fields')
const access = require('@condo/domains/ticket/access/TicketClassifierRule')


const TicketClassifierRule = new GQLListSchema('TicketClassifierRule', {
    // TODO(codegen): write doc for the TicketClassifierRule domain model!
    schemaDoc: 'TODO DOC!',
    fields: {
        dv: DV_FIELD,
        sender: SENDER_FIELD,

        organization: {
            // TODO(codegen): write doc for TicketClassifierRule.organization field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'Organization',
            kmigratorOptions: { null: true, on_delete: 'models.CASCADE' },
        },

        place: {
            // TODO(codegen): write doc for TicketClassifierRule.place field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'TicketPlaceClassifier',
            kmigratorOptions: { null: true, on_delete: 'models.PROTECT' },
        },

        category: {
            // TODO(codegen): write doc for TicketClassifierRule.category field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'TicketCategoryClassifier',
            kmigratorOptions: { null: true, on_delete: 'models.PROTECT' },
        },

        description: {
            // TODO(codegen): write doc for TicketClassifierRule.description field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'TicketDescriptionClassifier',
            kmigratorOptions: { null: true, on_delete: 'models.PROTECT' },
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), historical()],
    access: {
        read: access.canReadTicketClassifierRules,
        create: access.canManageTicketClassifierRules,
        update: access.canManageTicketClassifierRules,
        delete: false,
        auth: true,
    },
})

module.exports = {
    TicketClassifierRule,
}
