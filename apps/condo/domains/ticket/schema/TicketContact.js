/**
 * Generated by `createschema ticket.TicketContact 'property:Relationship:Property:PROTECT; unitName:Text; email:Text; phone:Text; name:Text;' --force`
 */

const { Text, Relationship, Integer, Select, Checkbox, DateTimeUtc, CalendarDay, Decimal, Password, File } = require('@keystonejs/fields')
const { Json } = require('@core/keystone/fields')
const { GQLListSchema } = require('@core/keystone/schema')
const { historical, versioned, uuided, tracked, softDeleted } = require('@core/keystone/plugins')
const { SENDER_FIELD, DV_FIELD } = require('@condo/domains/common/schema/fields')
const access = require('@condo/domains/ticket/access/TicketContact')


const TicketContact = new GQLListSchema('TicketContact', {
    // TODO(codegen): write doc for the TicketContact domain model!
    schemaDoc: 'TODO DOC!',
    fields: {
        dv: DV_FIELD,
        sender: SENDER_FIELD,

        property: {
            // TODO(codegen): write doc for TicketContact.property field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'Property',
            isRequired: true,
            knexOptions: { isNotNullable: true }, // Required relationship only!
            kmigratorOptions: { null: false, on_delete: 'models.PROTECT' },
        },

        unitName: {
            // TODO(codegen): write doc for TicketContact.unitName field!
            schemaDoc: 'TODO DOC!',
            type: Text,
            isRequired: true,
        },

        email: {
            // TODO(codegen): write doc for TicketContact.email field!
            schemaDoc: 'TODO DOC!',
            type: Text,
            isRequired: true,
        },

        phone: {
            // TODO(codegen): write doc for TicketContact.phone field!
            schemaDoc: 'TODO DOC!',
            type: Text,
            isRequired: true,
        },

        name: {
            // TODO(codegen): write doc for TicketContact.name field!
            schemaDoc: 'TODO DOC!',
            type: Text,
            isRequired: true,
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), historical()],
    access: {
        read: access.canReadTicketContacts,
        create: access.canManageTicketContacts,
        update: access.canManageTicketContacts,
        delete: false,
        auth: true,
    },
})

module.exports = {
    TicketContact,
}
