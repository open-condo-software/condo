/**
 * Generated by `createschema ticket.TicketFile 'file:File;ticket:Relationship:Ticket:PROTECT;name:Text;'`
 */

const { Text, Relationship, Integer, Select, Checkbox, DateTimeUtc, CalendarDay, Decimal, Password, File } = require('@keystonejs/fields')
const { Json } = require('@core/keystone/fields')
const { GQLListSchema } = require('@core/keystone/schema')
const { historical, versioned, uuided, tracked, softDeleted } = require('@core/keystone/plugins')
const { SENDER_FIELD, DV_FIELD } = require('@condo/domains/common/schema/fields')
const access = require('@condo/domains/ticket/access/TicketFile')
const { SberCloudFileAdapter } = require('@condo/domains/common/utils/sberCloudFileAdapter')

const fileAdapter = new SberCloudFileAdapter('ticket')


const TicketFile = new GQLListSchema('TicketFile', {
    // TODO(codegen): write doc for the TicketFile domain model!
    schemaDoc: 'TODO DOC!',
    fields: {
        dv: DV_FIELD,
        sender: SENDER_FIELD,

        file: {
            // TODO(codegen): write doc for TicketFile.file field!
            schemaDoc: 'TODO DOC!',
            type: File,
            adapter: fileAdapter,
            isRequired: true,
        },

        ticket: {
            // TODO(codegen): write doc for TicketFile.ticket field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'Ticket',
            isRequired: true,
            knexOptions: { isNotNullable: true }, // Required relationship only!
            kmigratorOptions: { null: false, on_delete: 'models.PROTECT' },
        },

        name: {
            // TODO(codegen): write doc for TicketFile.name field!
            schemaDoc: 'TODO DOC!',
            type: Text,
            isRequired: true,
        },
    },
    hooks: {
        afterDelete: async ({ existingItem }) => {
            if (existingItem.file) {
                await fileAdapter.delete(existingItem.file)
            }
        },
    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), historical()],
    access: {
        read: access.canReadTicketFiles,
        create: access.canManageTicketFiles,
        update: access.canManageTicketFiles,
        delete: false,
        auth: true,
    },
})

module.exports = {
    TicketFile,
}
