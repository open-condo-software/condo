/**
 * Generated by `createschema ticket.TicketDescriptionClassifier 'organization?:Relationship:Organization:CASCADE;name:Text;'`
 */

const { Text, Relationship, Integer, Select, Checkbox, DateTimeUtc, CalendarDay, Decimal, Password, File } = require('@keystonejs/fields')
const { Json } = require('@core/keystone/fields')
const { GQLListSchema } = require('@core/keystone/schema')
const { historical, versioned, uuided, tracked, softDeleted } = require('@core/keystone/plugins')
const { SENDER_FIELD, DV_FIELD } = require('@condo/domains/common/schema/fields')
const access = require('@condo/domains/ticket/access/TicketDescriptionClassifier')


const TicketDescriptionClassifier = new GQLListSchema('TicketDescriptionClassifier', {
    // TODO(codegen): write doc for the TicketDescriptionClassifier domain model!
    schemaDoc: 'TODO DOC!',
    fields: {
        dv: DV_FIELD,
        sender: SENDER_FIELD,

        organization: {
            // TODO(codegen): write doc for TicketDescriptionClassifier.organization field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'Organization',
            kmigratorOptions: { null: true, on_delete: 'models.CASCADE' },
        },

        name: {
            // TODO(codegen): write doc for TicketDescriptionClassifier.name field!
            schemaDoc: 'TODO DOC!',
            type: Text,
            isRequired: true,
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), historical()],
    access: {
        read: access.canReadTicketDescriptionClassifiers,
        create: access.canManageTicketDescriptionClassifiers,
        update: access.canManageTicketDescriptionClassifiers,
        delete: false,
        auth: true,
    },
})

module.exports = {
    TicketDescriptionClassifier,
}
