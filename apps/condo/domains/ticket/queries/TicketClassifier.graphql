query predictTicketClassification ($details: String!) {
    obj: predictTicketClassification(data: {
        details: $details,
    }) {
        id
        place {
            id
            name
        }
        category {
            id
            name
        }
    }
}