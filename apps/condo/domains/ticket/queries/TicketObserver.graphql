query getTicketObserversByTicketId ($ticketId: ID!) {
    observers: allTicketObservers (
        where: {
            ticket: {
                id: $ticketId
            }
        }
    ) {
        id
        user {
            id
        }
    }
}

 