// @ts-nocheck
/**
 * Generated by `createschema test.RelationModel 'name:Text;'`
 */

const { throwAuthenticationError } = require('@open-condo/keystone/apolloErrorFormatter')

async function canReadRelationModels ({ authentication: { item: user } }, asd, bsd, csd) {
    const a = 3

    return true
}

async function canManageRelationModels ({ authentication: { item: user }, originalInput, operation, itemId }) {
    return true
}

/*
  Rules are logical functions that used for list access, and may return a boolean (meaning
  all or no items are available) or a set of filters that limit the available items.
*/
module.exports = {
    canReadRelationModels,
    canManageRelationModels,
}
