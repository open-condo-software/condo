// @ts-nocheck
/**
 * Generated by `createschema service.Service 'name:Text; description?:Text; avatar?:Text; website?:Text; developer?:Text; type:Text; isHidden?:Checkbox; meta?:Json;'`
 */

const { throwAuthenticationError } = require("@condo/domains/common/utils/apolloErrorFormatter")

async function canReadServices ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false

    if (user.isAdmin) return {}

    // TODO(codegen): write canReadServices logic for user!
    return {}
}

async function canManageServices ({ authentication: { item: user }, originalInput, operation, itemId }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false
    if (user.isAdmin) return true

    if (operation === 'create') {
        // TODO(codegen): write canManageServices create logic!
        return true
    } else if (operation === 'update') {
        // TODO(codegen): write canManageServices update logic!
        return true
    }

    return false
}

/*
  Rules are logical functions that used for list access, and may return a boolean (meaning
  all or no items are available) or a set of filters that limit the available items.
*/
module.exports = {
    canReadServices,
    canManageServices,
}
