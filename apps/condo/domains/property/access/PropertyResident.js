/**
 * Generated by `createschema property.PropertyResident 'organization:Relationship:Organization:PROTECT; property:Relationship:Property:PROTECT; unitName:Text; name:Text; email:Text; phone:Text;'`
 */

async function canReadPropertyResidents ({ authentication: { item: user } }) {
    if (!user) return false
    if (user.isAdmin) return {}
    return {
        // TODO(codegen): write canReadPropertyResidents logic!
    }
}

async function canManagePropertyResidents ({ authentication: { item: user }, originalInput, operation, itemId }) {
    if (!user) return false
    if (user.isAdmin) return true
    if (operation === 'create') {
        // TODO(codegen): write canManagePropertyResidents create logic!
        return true
    } else if (operation === 'update') {
        // TODO(codegen): write canManagePropertyResidents update logic!
        return true
    }
    return false
}

/*
  Rules are logical functions that used for list access, and may return a boolean (meaning
  all or no items are available) or a set of filters that limit the available items.
*/
module.exports = {
    canReadPropertyResidents,
    canManagePropertyResidents,
}
