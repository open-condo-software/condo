/**
 * Generated by `createschema property.PropertyUnit 'property:Relationship:Property:PROTECT; map:Json'`
 */

async function canReadPropertyUnits ({ authentication: { item: user } }) {
    if (!user) return false
    if (user.isAdmin) return {}
    return {
        // TODO(codegen): write canReadPropertyUnits logic!
    }
}

async function canManagePropertyUnits ({ authentication: { item: user }, originalInput, operation, itemId }) {
    if (!user) return false
    if (user.isAdmin) return true
    if (operation === 'create') {
        // TODO(codegen): write canManagePropertyUnits create logic!
        return true
    } else if (operation === 'update') {
        // TODO(codegen): write canManagePropertyUnits update logic!
        return true
    }
    return false
}

/*
  Rules are logical functions that used for list access, and may return a boolean (meaning
  all or no items are available) or a set of filters that limit the available items.
*/
module.exports = {
    canReadPropertyUnits,
    canManagePropertyUnits,
}
