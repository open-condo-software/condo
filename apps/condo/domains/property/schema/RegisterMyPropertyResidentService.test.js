/**
 * Generated by `createservice property.RegisterMyPropertyResidentService --type mutations`
 */

const { makeLoggedInAdminClient, makeClient } = require('@core/keystone/test.utils')
const { expectToThrowAccessDeniedErrorToObj } = require('@condo/domains/common/utils/testSchema')

const { registerMyPropertyResidentByTestClient } = require('@condo/domains/property/utils/testSchema')
 
describe('RegisterMyPropertyResidentService', () => {
    test('user: execute', async () => {
        const client = await makeClient()  // TODO(codegen): use truly useful client!
        const payload = {}  // TODO(codegen): change the 'user: update RegisterMyPropertyResidentService' payload
        const [data, attrs] = await registerMyPropertyResidentByTestClient(client, payload)
        // TODO(codegen): write user expect logic
        throw new Error('Not implemented yet')
    })
 
    test('anonymous: execute', async () => {
        const client = await makeClient()
        await expectToThrowAccessDeniedErrorToObj(async () => {
            await registerMyPropertyResidentByTestClient(client)
        })
    })
 
    test('admin: execute', async () => {
        const admin = await makeLoggedInAdminClient()
        const payload = {}  // TODO(codegen): change the 'user: update RegisterMyPropertyResidentService' payload
        const [data, attrs] = await registerMyPropertyResidentByTestClient(admin, payload)
        // TODO(codegen): write admin expect logic
        throw new Error('Not implemented yet')
    })
})