/**
 * Generated by `createschema property.PropertyUnit 'property:Relationship:Property:PROTECT; map:Json'`
 */

const { Text, Relationship, Integer, Select, Checkbox, DateTimeUtc, CalendarDay, Decimal, Password, File } = require('@keystonejs/fields')
const { Json } = require('@core/keystone/fields')
const { GQLListSchema } = require('@core/keystone/schema')
const { historical, versioned, uuided, tracked, softDeleted } = require('@core/keystone/plugins')
const { SENDER_FIELD, DV_FIELD } = require('@condo/domains/common/schema/fields')
const access = require('@condo/domains/property/access/PropertyUnit')


const PropertyUnit = new GQLListSchema('PropertyUnit', {
    // TODO(codegen): write doc for the PropertyUnit domain model!
    schemaDoc: 'TODO DOC!',
    fields: {
        dv: DV_FIELD,
        sender: SENDER_FIELD,

        property: {
            // TODO(codegen): write doc for PropertyUnit.property field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'Property',
            isRequired: true,
            knexOptions: { isNotNullable: true }, // Required relationship only!
            kmigratorOptions: { null: false, on_delete: 'models.PROTECT' },
        },

        map: {
            // TODO(codegen): write doc for PropertyUnit.map field!
            schemaDoc: 'TODO DOC!',
            type: Json,
            isRequired: true,
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), historical()],
    access: {
        read: access.canReadPropertyUnits,
        create: access.canManagePropertyUnits,
        update: access.canManagePropertyUnits,
        delete: false,
        auth: true,
    },
})

module.exports = {
    PropertyUnit,
}
