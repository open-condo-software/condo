const MIN_SECTIONS_TO_SHOW_FILTER = 2

const BUILDING_EMPTY_MAP = {
    'dv': 1,
    'type': 'building',
    'sections': [],
}

module.exports = {
    MIN_SECTIONS_TO_SHOW_FILTER,
    BUILDING_EMPTY_MAP,
}
