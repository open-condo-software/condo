/**
 * Generated by `createschema property.PropertyUnit 'property:Relationship:Property:PROTECT; map:Json'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { PropertyUnit as PropertyUnitGQL } from '@condo/domains/property/gql'
import { PropertyUnit, PropertyUnitUpdateInput, QueryAllPropertyUnitsArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'property', 'map']
const RELATIONS = ['property']

export interface IPropertyUnitUIState extends PropertyUnit {
    id: string
    // TODO(codegen): write IPropertyUnitUIState or extends it from
}

function convertToUIState (item: PropertyUnit): IPropertyUnitUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IPropertyUnitUIState
}

export interface IPropertyUnitFormState {
    id?: undefined
    // TODO(codegen): write IPropertyUnitUIFormState or extends it from
}

function convertToUIFormState (state: IPropertyUnitUIState): IPropertyUnitFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IPropertyUnitFormState
}

function convertToGQLInput (state: IPropertyUnitFormState): PropertyUnitUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<PropertyUnit, PropertyUnitUpdateInput, IPropertyUnitFormState, IPropertyUnitUIState, QueryAllPropertyUnitsArgs>(PropertyUnitGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
