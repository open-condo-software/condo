/**
 * Generated by `createschema scope.SpecializationScope 'employee:Relationship:OrganizationEmployee:CASCADE; specialization:Relationship:TicketCategoryClassifier:CASCADE;'`
 */

const { Text, Relationship, Integer, Select, Checkbox, DateTimeUtc, CalendarDay, Decimal, Password, File, Url } = require('@keystonejs/fields')
const { Json } = require('@condo/keystone/fields')
const { GQLListSchema } = require('@condo/keystone/schema')
const { historical, versioned, uuided, tracked, softDeleted } = require('@condo/keystone/plugins')
const { dvAndSender } = require('@condo/domains/common/schema/plugins/dvAndSender')
const access = require('@condo/domains/scope/access/SpecializationScope')


const SpecializationScope = new GQLListSchema('SpecializationScope', {
    // TODO(codegen): write doc for the SpecializationScope domain model!
    schemaDoc: 'TODO DOC!',
    fields: {

        employee: {
            // TODO(codegen): write doc for SpecializationScope.employee field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'OrganizationEmployee',
            isRequired: true,
            knexOptions: { isNotNullable: true }, // Required relationship only!
            kmigratorOptions: { null: false, on_delete: 'models.CASCADE' },
        },

        specialization: {
            // TODO(codegen): write doc for SpecializationScope.specialization field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'TicketCategoryClassifier',
            isRequired: true,
            knexOptions: { isNotNullable: true }, // Required relationship only!
            kmigratorOptions: { null: false, on_delete: 'models.CASCADE' },
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), dvAndSender(), historical()],
    access: {
        read: access.canReadSpecializationScopes,
        create: access.canManageSpecializationScopes,
        update: access.canManageSpecializationScopes,
        delete: false,
        auth: true,
    },
})

module.exports = {
    SpecializationScope,
}
