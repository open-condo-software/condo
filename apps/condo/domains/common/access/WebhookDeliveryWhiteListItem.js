/**
 * Generated by `createschema common.WebhookDeliveryWhiteListItem`
 */

const { throwAuthenticationError } = require('@open-condo/keystone/apolloErrorFormatter')

async function canReadWebhookDeliveryWhiteListItems ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false

    // Only admin or support can read whitelist items
    if (user.isAdmin || user.isSupport) return {}

    return false
}

async function canManageWebhookDeliveryWhiteListItems ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false

    // Only admin or support can manage whitelist items
    return user.isAdmin || user.isSupport
}

module.exports = {
    canReadWebhookDeliveryWhiteListItems,
    canManageWebhookDeliveryWhiteListItems,
}
