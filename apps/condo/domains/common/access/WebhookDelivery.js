/**
 * Generated by `createschema common.WebhookDelivery`
 */

const { throwAuthenticationError } = require('@open-condo/keystone/apolloErrorFormatter')

async function canReadWebhookDeliveries ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false

    // Only admin or support can read webhook deliveries
    if (user.isAdmin || user.isSupport) return {}

    return false
}

async function canManageWebhookDeliveries ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false

    // Only admin or support can manage webhook deliveries
    return user.isAdmin || user.isSupport
}

module.exports = {
    canReadWebhookDeliveries,
    canManageWebhookDeliveries,
}
