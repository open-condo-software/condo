/**
 * Generated by `createservice subscription.ActivateTrialSubscriptionPlanService --type mutations`
 */

const { makeLoggedInAdminClient, makeClient } = require('@open-condo/keystone/test.utils')
const { expectToThrowAccessDeniedErrorToObj, expectToThrowAuthenticationErrorToObjects } = require('@open-condo/keystone/test.utils')

const { activateTrialSubscriptionPlanByTestClient } = require('@condo/domains/subscription/utils/testSchema')
 
describe('ActivateTrialSubscriptionPlanService', () => {
    test('user: execute', async () => {
        const client = await makeClient()  // TODO(codegen): use truly useful client!
        const payload = {}  // TODO(codegen): change the 'user: update ActivateTrialSubscriptionPlanService' payload
        const [data, attrs] = await activateTrialSubscriptionPlanByTestClient(client, payload)
        // TODO(codegen): write user expect logic
        throw new Error('Not implemented yet')
    })
 
    test('anonymous: execute', async () => {
        const client = await makeClient()
        await expectToThrowAuthenticationErrorToObjects(async () => {
            await activateTrialSubscriptionPlanByTestClient(client)
        })
    })
 
    test('admin: execute', async () => {
        const admin = await makeLoggedInAdminClient()
        const payload = {}  // TODO(codegen): change the 'user: update ActivateTrialSubscriptionPlanService' payload
        const [data, attrs] = await activateTrialSubscriptionPlanByTestClient(admin, payload)
        // TODO(codegen): write admin expect logic
        throw new Error('Not implemented yet')
    })
})