/**
 * Generated by `createschema document.Document 'organization:Relationship:Organization:CASCADE; property?:Relationship:Property:CASCADE; category:Relationship:PropertyFileCategory:CASCADE; file:File;'`
 */

const { Text, Relationship, Integer, Select, Checkbox, DateTimeUtc, CalendarDay, Decimal, Password, File, Url } = require('@keystonejs/fields')
const { Json } = require('@open-condo/keystone/fields')
const { GQLListSchema } = require('@open-condo/keystone/schema')
const { historical, versioned, uuided, tracked, softDeleted, dvAndSender } = require('@open-condo/keystone/plugins')
const access = require('@condo/domains/document/access/Document')


const Document = new GQLListSchema('Document', {
    // TODO(codegen): write doc for the Document domain model!
    schemaDoc: 'TODO DOC!',
    fields: {

        organization: {
            // TODO(codegen): write doc for Document.organization field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'Organization',
            isRequired: true,
            knexOptions: { isNotNullable: true }, // Required relationship only!
            kmigratorOptions: { null: false, on_delete: 'models.CASCADE' },
        },

        property: {
            // TODO(codegen): write doc for Document.property field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'Property',
            kmigratorOptions: { null: true, on_delete: 'models.CASCADE' },
        },

        category: {
            // TODO(codegen): write doc for Document.category field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'PropertyFileCategory',
            isRequired: true,
            knexOptions: { isNotNullable: true }, // Required relationship only!
            kmigratorOptions: { null: false, on_delete: 'models.CASCADE' },
        },

        file: {
            // TODO(codegen): write doc for Document.file field!
            schemaDoc: 'TODO DOC!',
            type: File,
            isRequired: true,
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), dvAndSender(), historical()],
    access: {
        read: access.canReadDocuments,
        create: access.canManageDocuments,
        update: access.canManageDocuments,
        delete: false,
        auth: true,
    },
})

module.exports = {
    Document,
}
