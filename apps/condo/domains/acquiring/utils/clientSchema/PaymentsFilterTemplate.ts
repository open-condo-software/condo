/**
 * Generated by `createschema acquiring.PaymentsFilterTemplate 'name:Text; employee:Relationship:OrganizationEmployee:CASCADE'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { PaymentsFilterTemplate as PaymentsFilterTemplateGQL } from '@condo/domains/acquiring/gql'
import { PaymentsFilterTemplate, PaymentsFilterTemplateUpdateInput, QueryAllPaymentsFilterTemplatesArgs } from '@app/condo/schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'name', 'employee', 'fields']
const RELATIONS = ['employee']

export interface IPaymentsFilterTemplateUIState extends PaymentsFilterTemplate {
    id: string
}

function convertToUIState (item: PaymentsFilterTemplate): IPaymentsFilterTemplateUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IPaymentsFilterTemplateUIState
}

export interface IPaymentsFilterTemplateFormState {
    id?: undefined
}

function convertToUIFormState (state: IPaymentsFilterTemplateUIState): IPaymentsFilterTemplateFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IPaymentsFilterTemplateFormState
}

function convertToGQLInput (state: IPaymentsFilterTemplateFormState): PaymentsFilterTemplateUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<PaymentsFilterTemplate, PaymentsFilterTemplateUpdateInput, IPaymentsFilterTemplateFormState, IPaymentsFilterTemplateUIState, QueryAllPaymentsFilterTemplatesArgs>(PaymentsFilterTemplateGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
