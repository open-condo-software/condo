/**
 * Generated by `createschema acquiring.PaymentRuleBillingScope 'property?:Relationship:Property:PROTECT;category?:Relationship:BillingCategory:PROTECT;bankAccountNumber?:Text;serviceId?:Text'`
 */

const { throwAuthenticationError } = require('@open-condo/keystone/apolloErrorFormatter')

async function canReadPaymentRuleBillingScopes ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false
    if (user.isAdmin || user.isSupport) return {}
    return false
}

async function canManagePaymentRuleBillingScopes ({ authentication: { item: user }, originalInput, operation, itemId }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false
    if (user.isAdmin) return true
    if (user.isSupport) {
        // Allow support user to delete payment rule billing scope
        if (operation === 'update' && originalInput.deletedAt) {
            return true
        }
    }
    return false
}

/*
  Rules are logical functions that used for list access, and may return a boolean (meaning
  all or no items are available) or a set of filters that limit the available items.
*/
module.exports = {
    canReadPaymentRuleBillingScopes,
    canManagePaymentRuleBillingScopes,
}
