/**
 * Generated by `createservice acquiring.GeneratePaymentLinkService`
 */
const { throwAuthenticationError } = require('@open-condo/keystone/apolloErrorFormatter')

const { STAFF, SERVICE } = require('@condo/domains/user/constants/common')

async function canGeneratePaymentLink ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()

    return user.isAdmin || user.type === STAFF || user.type === SERVICE
}

/*
  Rules are logical functions that used for list access, and may return a boolean (meaning
  all or no items are available) or a set of filters that limit the available items.
*/
module.exports = {
    canGeneratePaymentLink,
}