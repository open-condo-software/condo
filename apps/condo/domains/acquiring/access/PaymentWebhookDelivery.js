/**
 * Generated by `createschema acquiring.PaymentWebhookDelivery 'payment:Relationship:Payment:CASCADE; ...'`
 */

const { throwAuthenticationError } = require('@open-condo/keystone/apolloErrorFormatter')


/**
 * Read access: Only Admin or Support
 */
async function canReadPaymentWebhookDeliveries ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false

    // Only admin or support can read webhook deliveries
    if (user.isAdmin || user.isSupport) return {}

    return false
}

/**
 * Manage access: Only Admin or Support can create/update webhook deliveries
 * Regular users cannot manage webhook deliveries
 */
async function canManagePaymentWebhookDeliveries ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false

    // Only admin or support can manage webhook deliveries
    return user.isAdmin || user.isSupport
}

module.exports = {
    canReadPaymentWebhookDeliveries,
    canManagePaymentWebhookDeliveries,
}
