/**
 * Generated by `createschema acquiring.PaymentWebhookDeliveryWhiteListItem`
 */

const { throwAuthenticationError } = require('@open-condo/keystone/apolloErrorFormatter')


/**
 * Read access: Only Admin or Support
 */
async function canReadPaymentWebhookDeliveryWhiteListItems ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false

    // Only admin or support can read whitelist items
    if (user.isAdmin || user.isSupport) return {}

    return false
}

/**
 * Manage access: Only Admin or Support can create/update whitelist items
 * Regular users cannot manage the whitelist
 */
async function canManagePaymentWebhookDeliveryWhiteListItems ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false

    // Only admin or support can manage whitelist items
    return user.isAdmin || user.isSupport
}

module.exports = {
    canReadPaymentWebhookDeliveryWhiteListItems,
    canManagePaymentWebhookDeliveryWhiteListItems,
}
