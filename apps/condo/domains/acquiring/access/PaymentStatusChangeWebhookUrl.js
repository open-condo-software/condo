/**
 * Generated by `createschema acquiring.PaymentStatusChangeWebhookUrl`
 */

const { throwAuthenticationError } = require('@open-condo/keystone/apolloErrorFormatter')

async function canReadPaymentStatusChangeWebhookUrls ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false

    // Only admin or support can read payment webhook URLs
    if (user.isAdmin || user.isSupport) return {}

    return false
}

async function canManagePaymentStatusChangeWebhookUrls ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false

    // Only admin or support can manage payment webhook URLs
    return user.isAdmin || user.isSupport
}

module.exports = {
    canReadPaymentStatusChangeWebhookUrls,
    canManagePaymentStatusChangeWebhookUrls,
}
