/**
 * Generated by `createschema banking.BankCostItem 'name:Text;category:Relationship:BankCategory:SET_NULL'`
 */

const { Text, Relationship, Integer, Select, Checkbox, DateTimeUtc, CalendarDay, Decimal, Password, File, Url } = require('@keystonejs/fields')
const { Json } = require('@open-condo/keystone/fields')
const { GQLListSchema } = require('@open-condo/keystone/schema')
const { historical, versioned, uuided, tracked, softDeleted, dvAndSender } = require('@open-condo/keystone/plugins')
const access = require('@condo/domains/banking/access/BankCostItem')


const BankCostItem = new GQLListSchema('BankCostItem', {
    // TODO(codegen): write doc for the BankCostItem domain model!
    schemaDoc: 'TODO DOC!',
    fields: {

        name: {
            // TODO(codegen): write doc for BankCostItem.name field!
            schemaDoc: 'TODO DOC!',
            type: Text,
            isRequired: true,
        },

        category: {
            // TODO(codegen): write doc for BankCostItem.category field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'BankCategory',
            isRequired: true,
            knexOptions: { isNotNullable: true }, // Required relationship only!
            kmigratorOptions: { null: false, on_delete: 'models.SET_NULL' },
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), dvAndSender(), historical()],
    access: {
        read: access.canReadBankCostItems,
        create: access.canManageBankCostItems,
        update: access.canManageBankCostItems,
        delete: false,
        auth: true,
    },
})

module.exports = {
    BankCostItem,
}
