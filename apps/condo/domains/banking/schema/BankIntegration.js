/**
 * Generated by `createschema banking.BankIntegration 'name:Text'`
 */

const { Text, Relationship, Integer, Select, Checkbox, DateTimeUtc, CalendarDay, Decimal, Password, File, Url } = require('@keystonejs/fields')
const { Json } = require('@open-condo/keystone/fields')
const { GQLListSchema } = require('@open-condo/keystone/schema')
const { historical, versioned, uuided, tracked, softDeleted, dvAndSender } = require('@open-condo/keystone/plugins')
const access = require('@condo/domains/banking/access/BankIntegration')


const BankIntegration = new GQLListSchema('BankIntegration', {
    // TODO(codegen): write doc for the BankIntegration domain model!
    schemaDoc: 'TODO DOC!',
    fields: {

        name: {
            // TODO(codegen): write doc for BankIntegration.name field!
            schemaDoc: 'TODO DOC!',
            type: Text,
            isRequired: true,
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), dvAndSender(), historical()],
    access: {
        read: access.canReadBankIntegrations,
        create: access.canManageBankIntegrations,
        update: access.canManageBankIntegrations,
        delete: false,
        auth: true,
    },
})

module.exports = {
    BankIntegration,
}
