/**
 * Generated by `createservice notification._internalSendHashedResidentPhonesService '--type=mutations'`
 */

const { makeLoggedInAdminClient, makeClient } = require('@open-condo/keystone/test.utils')
const { expectToThrowAccessDeniedErrorToObj, expectToThrowAuthenticationErrorToObjects } = require('@open-condo/keystone/test.utils')

const { _internalSendHashedResidentPhonesByTestClient } = require('@condo/domains/notification/utils/testSchema')
 
describe('_internalSendHashedResidentPhonesService', () => {

    // check that user with directAccess can execute

    // check that user without directAccess can not execute

    test('user: execute', async () => {
        const client = await makeClient()
        const payload = {}
        const [data] = await _internalSendHashedResidentPhonesByTestClient(client, payload)

        console.log(data)
    })
})