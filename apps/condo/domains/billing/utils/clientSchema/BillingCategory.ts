/**
 * Generated by `createschema billing.BillingCategory 'name:Text'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { BillingCategory as BillingCategoryGQL } from '@condo/domains/billing/gql'
import { BillingCategory, BillingCategoryUpdateInput, QueryAllBillingCategoriesArgs } from '@app/condo/schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'name']
const RELATIONS = []

export interface IBillingCategoryUIState extends BillingCategory {
    id: string
    // TODO(codegen): write IBillingCategoryUIState or extends it from
}

function convertToUIState (item: BillingCategory): IBillingCategoryUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IBillingCategoryUIState
}

export interface IBillingCategoryFormState {
    id?: undefined
    // TODO(codegen): write IBillingCategoryUIFormState or extends it from
}

function convertToUIFormState (state: IBillingCategoryUIState): IBillingCategoryFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IBillingCategoryFormState
}

function convertToGQLInput (state: IBillingCategoryFormState): BillingCategoryUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<BillingCategory, BillingCategoryUpdateInput, IBillingCategoryFormState, IBillingCategoryUIState, QueryAllBillingCategoriesArgs>(BillingCategoryGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
