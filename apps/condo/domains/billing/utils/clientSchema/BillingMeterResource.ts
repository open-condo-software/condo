/**
 * Generated by `createschema billing.BillingMeterResource 'name:Text'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { BillingMeterResource as BillingMeterResourceGQL } from '@condo/domains/billing/gql'
import { BillingMeterResource, BillingMeterResourceUpdateInput, QueryAllBillingMeterResourcesArgs } from '@app/condo/schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'name']
const RELATIONS = []

export interface IBillingMeterResourceUIState extends BillingMeterResource {
    id: string
    // TODO(codegen): write IBillingMeterResourceUIState or extends it from
}

function convertToUIState(item: BillingMeterResource): IBillingMeterResourceUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IBillingMeterResourceUIState
}

export interface IBillingMeterResourceFormState {
    id?: undefined
    // TODO(codegen): write IBillingMeterResourceUIFormState or extends it from
}

function convertToUIFormState(state: IBillingMeterResourceUIState): IBillingMeterResourceFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = RELATIONS.includes(attr) && state[attr] ? attrId || state[attr] : state[attr]
    }
    return result as IBillingMeterResourceFormState
}

function convertToGQLInput(state: IBillingMeterResourceFormState): BillingMeterResourceUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = RELATIONS.includes(attr) && state[attr] ? { connect: { id: attrId || state[attr] } } : state[attr]
    }
    return result
}

const { useObject, useObjects, useCreate, useUpdate, useDelete } = generateReactHooks<
    BillingMeterResource,
    BillingMeterResourceUpdateInput,
    IBillingMeterResourceFormState,
    IBillingMeterResourceUIState,
    QueryAllBillingMeterResourcesArgs
>(BillingMeterResourceGQL, { convertToGQLInput, convertToUIState })

export { useObject, useObjects, useCreate, useUpdate, useDelete, convertToUIFormState }
