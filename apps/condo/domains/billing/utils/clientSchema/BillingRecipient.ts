/**
 * Generated by `createschema billing.BillingRecipient 'organization:Relationship:Organization:CASCADE; tin:Text; iec:Text; bic:Text; context?:Relationship:BillingIntegrationOrganizationContext:SET_NULL; bankAccount:Text; name?:Text; approved:Checkbox; meta?:Json;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { BillingRecipient as BillingRecipientGQL } from '@condo/domains/billing/gql'
import { BillingRecipient, BillingRecipientUpdateInput, QueryAllBillingRecipientsArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'organization', 'tin', 'iec', 'bic', 'context', 'bankAccount', 'name', 'approved', 'meta']
const RELATIONS = ['organization', 'context']

export interface IBillingRecipientUIState extends BillingRecipient {
    id: string
    // TODO(codegen): write IBillingRecipientUIState or extends it from
}

function convertToUIState (item: BillingRecipient): IBillingRecipientUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IBillingRecipientUIState
}

export interface IBillingRecipientFormState {
    id?: undefined
    // TODO(codegen): write IBillingRecipientUIFormState or extends it from
}

function convertToUIFormState (state: IBillingRecipientUIState): IBillingRecipientFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IBillingRecipientFormState
}

function convertToGQLInput (state: IBillingRecipientFormState): BillingRecipientUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<BillingRecipient, BillingRecipientUpdateInput, IBillingRecipientFormState, IBillingRecipientUIState, QueryAllBillingRecipientsArgs>(BillingRecipientGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
