/**
 * Generated by `createservice billing.CheckOrganizationIntegrationContextExistService --type queries`
 */

const { makeLoggedInAdminClient, makeClient } = require('@core/keystone/test.utils')
const { expectToThrowAccessDeniedErrorToObj } = require('@condo/domains/common/utils/testSchema')

const { checkOrganizationIntegrationContextExistByTestClient } = require('@condo/domains/billing/utils/testSchema')
 
describe('CheckOrganizationIntegrationContextExistService', () => {
    test('user: execute', async () => {
        const client = await makeClient()  // TODO(codegen): use truly useful client!
        const payload = {}  // TODO(codegen): change the 'user: update CheckOrganizationIntegrationContextExistService' payload
        const [data, attrs] = await checkOrganizationIntegrationContextExistByTestClient(client, payload)
        // TODO(codegen): write user expect logic
        throw new Error('Not implemented yet')
    })
 
    test('anonymous: execute', async () => {
        const client = await makeClient()
        await expectToThrowAuthenticationErrorToObjects(async () => {
            await checkOrganizationIntegrationContextExistByTestClient(client)
        })
    })
 
    test('admin: execute', async () => {
        const admin = await makeLoggedInAdminClient()
        const payload = {}  // TODO(codegen): change the 'user: update CheckOrganizationIntegrationContextExistService' payload
        const [data, attrs] = await checkOrganizationIntegrationContextExistByTestClient(admin, payload)
        // TODO(codegen): write admin expect logic
        throw new Error('Not implemented yet')
    })
})