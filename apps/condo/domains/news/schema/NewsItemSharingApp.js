/**
 * Generated by `createschema news.NewsItemSharingProvider 'b2bApp:Relationship:B2BApp:CASCADE; name:Text; newsItemPostUrl:Text; newsItemPreviewUrl:Text; newsItemSendingFeatures:Json;'`
 */

const { Text, Relationship, Integer, Select, Checkbox, DateTimeUtc, CalendarDay, Decimal, Password, File, Url } = require('@keystonejs/fields')

const { Json } = require('@open-condo/keystone/fields')
const { historical, versioned, uuided, tracked, softDeleted, dvAndSender } = require('@open-condo/keystone/plugins')
const { GQLListSchema } = require('@open-condo/keystone/schema')

const access = require('@condo/domains/news/access/NewsItemSharingProvider')


const NewsItemSharingApp = new GQLListSchema('NewsItemSharingApp', {
    // TODO(codegen): write doc for the NewsItemSharingProvider domain model!
    schemaDoc: 'TODO DOC!',
    fields: {

        b2bApp: {
            // TODO(codegen): write doc for NewsItemSharingProvider.b2bApp field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'B2BApp',
            isRequired: true,
            knexOptions: { isNotNullable: true }, // Required relationship only!
            kmigratorOptions: { null: false, on_delete: 'models.CASCADE' },
        },

        name: {
            // TODO(codegen): write doc for NewsItemSharingProvider.name field!
            schemaDoc: 'TODO DOC!',
            type: Text,
            isRequired: true,
        },

        newsItemPostUrl: {
            // TODO(codegen): write doc for NewsItemSharingProvider.newsItemPostUrl field!
            schemaDoc: 'TODO DOC!',
            type: Text,
            isRequired: true,
        },

        newsItemPreviewUrl: {
            // TODO(codegen): write doc for NewsItemSharingProvider.newsItemPreviewUrl field!
            schemaDoc: 'TODO DOC!',
            type: Text,
            isRequired: true,
        },

        newsItemSendingFeatures: {
            // TODO(codegen): write doc for NewsItemSharingProvider.newsItemSendingFeatures field!
            schemaDoc: 'TODO DOC!',
            type: Json,
            isRequired: true,
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), dvAndSender(), historical()],
    access: {
        read: access.canReadNewsItemSharingProviders,
        create: access.canManageNewsItemSharingProviders,
        update: access.canManageNewsItemSharingProviders,
        delete: false,
        auth: true,
    },
})

module.exports = {
    NewsItemSharingApp,
}
