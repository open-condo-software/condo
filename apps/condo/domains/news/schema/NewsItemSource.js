/**
 * Generated by `createschema news.NewsItemSource 'name:Text; key:Text; isDefault:Checkbox'`
 */

const { readOnlyFieldAccess } = require('@open-condo/keystone/access')
const { historical, versioned, uuided, tracked, softDeleted, dvAndSender, analytical } = require('@open-condo/keystone/plugins')
const { GQLListSchema } = require('@open-condo/keystone/schema')

const access = require('@condo/domains/news/access/NewsItemSource')
const { NEWS_ITEM_SOURCE_TYPES } = require('@condo/domains/news/constants/newsItemSourceTypes')


const NewsItemSource = new GQLListSchema('NewsItemSource', {
    schemaDoc: 'News item source. Web app (news form), external system, ...',
    fields: {
        type: {
            type: 'Select',
            isRequired: true,
            options: Object.values(NEWS_ITEM_SOURCE_TYPES),
        },

        name: {
            schemaDoc: 'Localized News item source name',
            type: 'LocalizedText',
            isRequired: true,
            template: 'news.source.*.name',
        },

        isDefault: {
            schemaDoc: '(Read-only) Indicates whether a news source is default or custom.',
            type: 'Checkbox',
            defaultValue: false,
            isRequired: true,
            kmigratorOptions: { default: false },
            access: readOnlyFieldAccess,
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), dvAndSender(), historical(), analytical()],
    access: {
        read: access.canReadNewsItemSources,
        create: access.canManageNewsItemSources,
        update: access.canManageNewsItemSources,
        delete: false,
        auth: true,
    },
    escapeSearch: true,
})

module.exports = {
    NewsItemSource,
}
