/**
 * Generated by `createschema meter.MeterReadingTicketComment 'meterReadingTicket:Relationship:MeterReadingTicket:CASCADE; user:Relationship:User:CASCADE; content:Text;'`
 */

const { Text, Relationship, Integer, Select, Checkbox, DateTimeUtc, CalendarDay, Decimal, Password, File } = require('@keystonejs/fields')
const { Json } = require('@core/keystone/fields')
const { GQLListSchema } = require('@core/keystone/schema')
const { historical, versioned, uuided, tracked, softDeleted } = require('@core/keystone/plugins')
const { SENDER_FIELD, DV_FIELD } = require('@condo/domains/common/schema/fields')
const access = require('@condo/domains/meter/access/MeterReadingTicketComment')


const MeterReadingTicketComment = new GQLListSchema('MeterReadingTicketComment', {
    // TODO(codegen): write doc for the MeterReadingTicketComment domain model!
    schemaDoc: 'TODO DOC!',
    fields: {
        dv: DV_FIELD,
        sender: SENDER_FIELD,

        meterReadingTicket: {
            // TODO(codegen): write doc for MeterReadingTicketComment.meterReadingTicket field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'MeterReadingTicket',
            isRequired: true,
            knexOptions: { isNotNullable: true }, // Required relationship only!
            kmigratorOptions: { null: false, on_delete: 'models.CASCADE' },
        },

        user: {
            // TODO(codegen): write doc for MeterReadingTicketComment.user field!
            schemaDoc: 'TODO DOC!',
            type: Relationship,
            ref: 'User',
            isRequired: true,
            knexOptions: { isNotNullable: true }, // Required relationship only!
            kmigratorOptions: { null: false, on_delete: 'models.CASCADE' },
        },

        content: {
            // TODO(codegen): write doc for MeterReadingTicketComment.content field!
            schemaDoc: 'TODO DOC!',
            type: Text,
            isRequired: true,
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), historical()],
    access: {
        read: access.canReadMeterReadingTicketComments,
        create: access.canManageMeterReadingTicketComments,
        update: access.canManageMeterReadingTicketComments,
        delete: false,
        auth: true,
    },
})

module.exports = {
    MeterReadingTicketComment,
}
