/**
 * Generated by `createschema meter.MeterSource 'organization:Relationship:Organization:CASCADE; type:Select:call,mobile_app,billing; name:Text;'`
 */

const { Text, Relationship, Integer, Select, Checkbox, DateTimeUtc, CalendarDay, Decimal, Password, File } = require('@keystonejs/fields')
const { Json } = require('@core/keystone/fields')
const { GQLListSchema } = require('@core/keystone/schema')
const { historical, versioned, uuided, tracked, softDeleted } = require('@core/keystone/plugins')
const { SENDER_FIELD, DV_FIELD } = require('@condo/domains/common/schema/fields')
const access = require('@condo/domains/meter/access/MeterSource')
const { COMMON_AND_ORGANIZATION_OWNED_FIELD } = require('../../../schema/_common')


const MeterSource = new GQLListSchema('MeterSource', {
    schemaDoc: 'Where did the data for meter readings come from',
    fields: {
        dv: DV_FIELD,
        sender: SENDER_FIELD,

        organization: COMMON_AND_ORGANIZATION_OWNED_FIELD,

        type: {
            schemaDoc: 'Source type',
            type: Select,
            options: 'call, mobile_app, billing',
            isRequired: true,
        },

        name: {
            schemaDoc: 'Readable title for the source',
            type: Text,
            isRequired: true,
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), historical()],
    access: {
        read: access.canReadMeterSources,
        create: access.canManageMeterSources,
        update: access.canManageMeterSources,
        delete: false,
        auth: true,
    },
})

module.exports = {
    MeterSource,
}
