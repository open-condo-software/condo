/**
 * Generated by `createschema meter.MeterReadingTicketChange 'meterReadingTicket:Relationship:MeterReadingTicket:CASCADE;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { MeterReadingTicketChange as MeterReadingTicketChangeGQL } from '@condo/domains/meter/gql'
import { MeterReadingTicketChange, MeterReadingTicketChangeUpdateInput, QueryAllMeterReadingTicketChangesArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'meterReadingTicket']
const RELATIONS = ['meterReadingTicket']

export interface IMeterReadingTicketChangeUIState extends MeterReadingTicketChange {
    id: string
    // TODO(codegen): write IMeterReadingTicketChangeUIState or extends it from
}

function convertToUIState (item: MeterReadingTicketChange): IMeterReadingTicketChangeUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IMeterReadingTicketChangeUIState
}

export interface IMeterReadingTicketChangeFormState {
    id?: undefined
    // TODO(codegen): write IMeterReadingTicketChangeUIFormState or extends it from
}

function convertToUIFormState (state: IMeterReadingTicketChangeUIState): IMeterReadingTicketChangeFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IMeterReadingTicketChangeFormState
}

function convertToGQLInput (state: IMeterReadingTicketChangeFormState): MeterReadingTicketChangeUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<MeterReadingTicketChange, MeterReadingTicketChangeUpdateInput, IMeterReadingTicketChangeFormState, IMeterReadingTicketChangeUIState, QueryAllMeterReadingTicketChangesArgs>(MeterReadingTicketChangeGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
