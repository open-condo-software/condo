/**
 * Generated by `createschema meter.MeterSource 'type:Select:call,mobile_app,billing; name:Text;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { MeterSource as MeterSourceGQL } from '@condo/domains/meter/gql'
import { MeterSource, MeterSourceUpdateInput, QueryAllMeterSourcesArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'type', 'name']
const RELATIONS = []

export interface IMeterSourceUIState extends MeterSource {
    id: string
    // TODO(codegen): write IMeterSourceUIState or extends it from
}

function convertToUIState (item: MeterSource): IMeterSourceUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IMeterSourceUIState
}

export interface IMeterSourceFormState {
    id?: undefined
    // TODO(codegen): write IMeterSourceUIFormState or extends it from
}

function convertToUIFormState (state: IMeterSourceUIState): IMeterSourceFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IMeterSourceFormState
}

function convertToGQLInput (state: IMeterSourceFormState): MeterSourceUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<MeterSource, MeterSourceUpdateInput, IMeterSourceFormState, IMeterSourceUIState, QueryAllMeterSourcesArgs>(MeterSourceGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
