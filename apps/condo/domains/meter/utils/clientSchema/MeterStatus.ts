/**
 * Generated by `createschema meter.MeterStatus 'organization:Relationship:Organization:CASCADE; type:Select:new_or_reopened,processing,canceled,completed,deferred,closed; name:Text;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { MeterStatus as MeterStatusGQL } from '@condo/domains/meter/gql'
import { MeterStatus, MeterStatusUpdateInput, QueryAllMeterStatusesArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'organization', 'type', 'name']
const RELATIONS = ['organization']

export interface IMeterStatusUIState extends MeterStatus {
    id: string
    // TODO(codegen): write IMeterStatusUIState or extends it from
}

function convertToUIState (item: MeterStatus): IMeterStatusUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IMeterStatusUIState
}

export interface IMeterStatusFormState {
    id?: undefined
    // TODO(codegen): write IMeterStatusUIFormState or extends it from
}

function convertToUIFormState (state: IMeterStatusUIState): IMeterStatusFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IMeterStatusFormState
}

function convertToGQLInput (state: IMeterStatusFormState): MeterStatusUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<MeterStatus, MeterStatusUpdateInput, IMeterStatusFormState, IMeterStatusUIState, QueryAllMeterStatusesArgs>(MeterStatusGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
