/**
 * Generated by `createschema meter.MeterReadingTicketComment 'meterReadingTicket:Relationship:MeterReadingTicket:CASCADE; user:Relationship:User:CASCADE; content:Text;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { MeterReadingTicketComment as MeterReadingTicketCommentGQL } from '@condo/domains/meter/gql'
import { MeterReadingTicketComment, MeterReadingTicketCommentUpdateInput, QueryAllMeterReadingTicketCommentsArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'meterReadingTicket', 'user', 'content']
const RELATIONS = ['meterReadingTicket', 'user']

export interface IMeterReadingTicketCommentUIState extends MeterReadingTicketComment {
    id: string
    // TODO(codegen): write IMeterReadingTicketCommentUIState or extends it from
}

function convertToUIState (item: MeterReadingTicketComment): IMeterReadingTicketCommentUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IMeterReadingTicketCommentUIState
}

export interface IMeterReadingTicketCommentFormState {
    id?: undefined
    // TODO(codegen): write IMeterReadingTicketCommentUIFormState or extends it from
}

function convertToUIFormState (state: IMeterReadingTicketCommentUIState): IMeterReadingTicketCommentFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IMeterReadingTicketCommentFormState
}

function convertToGQLInput (state: IMeterReadingTicketCommentFormState): MeterReadingTicketCommentUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<MeterReadingTicketComment, MeterReadingTicketCommentUpdateInput, IMeterReadingTicketCommentFormState, IMeterReadingTicketCommentUIState, QueryAllMeterReadingTicketCommentsArgs>(MeterReadingTicketCommentGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
