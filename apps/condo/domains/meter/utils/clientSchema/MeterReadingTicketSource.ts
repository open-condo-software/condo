/**
 * Generated by `createschema meter.MeterReadingTicketSource 'organization:Relationship:Organization:CASCADE; type:Select:call,mobile_app,billing; name:Text;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { MeterReadingTicketSource as MeterReadingTicketSourceGQL } from '@condo/domains/meter/gql'
import { MeterReadingTicketSource, MeterReadingTicketSourceUpdateInput, QueryAllMeterReadingTicketSourcesArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'organization', 'type', 'name']
const RELATIONS = ['organization']

export interface IMeterReadingTicketSourceUIState extends MeterReadingTicketSource {
    id: string
    // TODO(codegen): write IMeterReadingTicketSourceUIState or extends it from
}

function convertToUIState (item: MeterReadingTicketSource): IMeterReadingTicketSourceUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IMeterReadingTicketSourceUIState
}

export interface IMeterReadingTicketSourceFormState {
    id?: undefined
    // TODO(codegen): write IMeterReadingTicketSourceUIFormState or extends it from
}

function convertToUIFormState (state: IMeterReadingTicketSourceUIState): IMeterReadingTicketSourceFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IMeterReadingTicketSourceFormState
}

function convertToGQLInput (state: IMeterReadingTicketSourceFormState): MeterReadingTicketSourceUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<MeterReadingTicketSource, MeterReadingTicketSourceUpdateInput, IMeterReadingTicketSourceFormState, IMeterReadingTicketSourceUIState, QueryAllMeterReadingTicketSourcesArgs>(MeterReadingTicketSourceGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
