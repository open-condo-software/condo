/**
 * Generated by `createschema meter.MeterReadingSource 'type:Select:call,mobile_app,billing; name:Text;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'
import { generateNewReactHooks } from '@condo/domains/common/utils/codegeneration/new.generate.hooks'

import { MeterReadingSource as MeterReadingSourceGQL } from '@condo/domains/meter/gql'
import { MeterReadingSource, MeterReadingSourceCreateInput, MeterReadingSourceUpdateInput, QueryAllMeterReadingSourcesArgs } from '@app/condo/schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'type', 'name']
const RELATIONS = []

export interface IMeterReadingSourceUIState extends MeterReadingSource {
    id: string
}

function convertToUIState (item: MeterReadingSource): IMeterReadingSourceUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IMeterReadingSourceUIState
}

export interface IMeterReadingSourceFormState {
    id?: undefined
}

function convertToUIFormState (state: IMeterReadingSourceUIState): IMeterReadingSourceFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IMeterReadingSourceFormState
}

function convertToGQLInput (state: IMeterReadingSourceFormState): MeterReadingSourceUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<MeterReadingSource, MeterReadingSourceUpdateInput, IMeterReadingSourceFormState, IMeterReadingSourceUIState, QueryAllMeterReadingSourcesArgs>(MeterReadingSourceGQL, { convertToGQLInput, convertToUIState })
const {
    useNewObject,
    useNewObjects,
    useNewCreate,
    useNewUpdate,
    useNewSoftDelete,
} = generateNewReactHooks<MeterReadingSource, MeterReadingSourceCreateInput, MeterReadingSourceUpdateInput, QueryAllMeterReadingSourcesArgs>(MeterReadingSourceGQL)

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
    useNewObject,
    useNewObjects,
    useNewCreate,
    useNewUpdate,
    useNewSoftDelete,
}
