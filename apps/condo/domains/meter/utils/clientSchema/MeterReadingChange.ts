/**
 * Generated by `createschema meter.MeterReadingChange 'meterReading:Relationship:MeterReading:CASCADE;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { MeterReadingChange as MeterReadingChangeGQL } from '@condo/domains/meter/gql'
import { MeterReadingChange, MeterReadingChangeUpdateInput, QueryAllMeterReadingChangesArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'meterReading']
const RELATIONS = ['meterReading']

export interface IMeterReadingChangeUIState extends MeterReadingChange {
    id: string
    // TODO(codegen): write IMeterReadingChangeUIState or extends it from
}

function convertToUIState (item: MeterReadingChange): IMeterReadingChangeUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IMeterReadingChangeUIState
}

export interface IMeterReadingChangeFormState {
    id?: undefined
    // TODO(codegen): write IMeterReadingChangeUIFormState or extends it from
}

function convertToUIFormState (state: IMeterReadingChangeUIState): IMeterReadingChangeFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IMeterReadingChangeFormState
}

function convertToGQLInput (state: IMeterReadingChangeFormState): MeterReadingChangeUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<MeterReadingChange, MeterReadingChangeUpdateInput, IMeterReadingChangeFormState, IMeterReadingChangeUIState, QueryAllMeterReadingChangesArgs>(MeterReadingChangeGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
