// @ts-nocheck
/**
 * Generated by `createschema meter.MeterReadingTicketComment 'meterReadingTicket:Relationship:MeterReadingTicket:CASCADE; user:Relationship:User:CASCADE; content:Text;'`
 */

const { throwAuthenticationError } = require("@condo/domains/common/utils/apolloErrorFormatter")

async function canReadMeterReadingTicketComments ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.isAdmin) return {}
    return {
        // TODO(codegen): write canReadMeterReadingTicketComments logic!
    }
}

async function canManageMeterReadingTicketComments ({ authentication: { item: user }, originalInput, operation, itemId }) {
    if (!user) return throwAuthenticationError()
    if (user.isAdmin) return true
    if (operation === 'create') {
        // TODO(codegen): write canManageMeterReadingTicketComments create logic!
        return true
    } else if (operation === 'update') {
        // TODO(codegen): write canManageMeterReadingTicketComments update logic!
        return true
    }
    return false
}

/*
  Rules are logical functions that used for list access, and may return a boolean (meaning
  all or no items are available) or a set of filters that limit the available items.
*/
module.exports = {
    canReadMeterReadingTicketComments,
    canManageMeterReadingTicketComments,
}
