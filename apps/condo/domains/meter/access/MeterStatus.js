// @ts-nocheck
/**
 * Generated by `createschema meter.MeterStatus 'organization:Relationship:Organization:CASCADE; type:Select:new_or_reopened,processing,canceled,completed,deferred,closed; name:Text;'`
 */

const { throwAuthenticationError } = require("@condo/domains/common/utils/apolloErrorFormatter")

async function canReadMeterStatuses ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.isAdmin) return {}
    return {
        // TODO(codegen): write canReadMeterStatuses logic!
    }
}

async function canManageMeterStatuses ({ authentication: { item: user }, originalInput, operation, itemId }) {
    if (!user) return throwAuthenticationError()
    if (user.isAdmin) return true
    if (operation === 'create') {
        // TODO(codegen): write canManageMeterStatuses create logic!
        return true
    } else if (operation === 'update') {
        // TODO(codegen): write canManageMeterStatuses update logic!
        return true
    }
    return false
}

/*
  Rules are logical functions that used for list access, and may return a boolean (meaning
  all or no items are available) or a set of filters that limit the available items.
*/
module.exports = {
    canReadMeterStatuses,
    canManageMeterStatuses,
}
