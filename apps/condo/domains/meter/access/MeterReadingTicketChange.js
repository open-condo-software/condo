// @ts-nocheck
/**
 * Generated by `createschema meter.MeterReadingTicketChange 'meterReadingTicket:Relationship:MeterReadingTicket:CASCADE;'`
 */

const { throwAuthenticationError } = require("@condo/domains/common/utils/apolloErrorFormatter")

async function canReadMeterReadingTicketChanges ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.isAdmin) return {}
    return {
        // TODO(codegen): write canReadMeterReadingTicketChanges logic!
    }
}

async function canManageMeterReadingTicketChanges ({ authentication: { item: user }, originalInput, operation, itemId }) {
    if (!user) return throwAuthenticationError()
    if (user.isAdmin) return true
    if (operation === 'create') {
        // TODO(codegen): write canManageMeterReadingTicketChanges create logic!
        return true
    } else if (operation === 'update') {
        // TODO(codegen): write canManageMeterReadingTicketChanges update logic!
        return true
    }
    return false
}

/*
  Rules are logical functions that used for list access, and may return a boolean (meaning
  all or no items are available) or a set of filters that limit the available items.
*/
module.exports = {
    canReadMeterReadingTicketChanges,
    canManageMeterReadingTicketChanges,
}
