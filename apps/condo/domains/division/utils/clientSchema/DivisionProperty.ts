/**
 * Generated by `createschema division.DivisionProperty 'division:Relationship:Division:CASCADE; property:Relationship:Property:CASCADE;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { DivisionProperty as DivisionPropertyGQL } from '@condo/domains/division/gql'
import { DivisionProperty, DivisionPropertyUpdateInput, QueryAllDivisionPropertiesArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'division', 'property']
const RELATIONS = ['division', 'property']

export interface IDivisionPropertyUIState extends DivisionProperty {
    id: string
    // TODO(codegen): write IDivisionPropertyUIState or extends it from
}

function convertToUIState (item: DivisionProperty): IDivisionPropertyUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IDivisionPropertyUIState
}

export interface IDivisionPropertyFormState {
    id?: undefined
    // TODO(codegen): write IDivisionPropertyUIFormState or extends it from
}

function convertToUIFormState (state: IDivisionPropertyUIState): IDivisionPropertyFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IDivisionPropertyFormState
}

function convertToGQLInput (state: IDivisionPropertyFormState): DivisionPropertyUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<DivisionProperty, DivisionPropertyUpdateInput, IDivisionPropertyFormState, IDivisionPropertyUIState, QueryAllDivisionPropertiesArgs>(DivisionPropertyGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
