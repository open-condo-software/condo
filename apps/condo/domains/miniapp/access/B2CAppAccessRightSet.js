/**
 * Generated by `createschema miniapp.B2CAppAccessRightSet 'app:Relationship:B2CApp:SET_NULL'`
 */

const { throwAuthenticationError } = require('@open-condo/keystone/apolloErrorFormatter')

const { SERVICE } = require('../../user/constants/common')
const { canDirectlyManageSchemaObjects, canDirectlyReadSchemaObjects } = require('../../user/utils/directAccess')

async function canReadB2CAppAccessRightSets ({ authentication: { item: user }, listKey }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false

    if (user.isAdmin || user.isSupport) return {}

    const hasDirectAccess = await canDirectlyReadSchemaObjects(user, listKey)
    if (hasDirectAccess) return true

    if (user.type === SERVICE) {
        return {
            app: {
                accessRights_some: {
                    user: {
                        id: user.id,
                        deletedAt: null,
                    },
                    deletedAt: null,
                },
                deletedAt: null,
            },
        }
    }
    
    return false
}

async function canManageB2CAppAccessRightSets ({ authentication: { item: user }, originalInput, operation, listKey }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false
    if (user.isAdmin || user.isSupport) return true

    return await canDirectlyManageSchemaObjects(user, listKey, originalInput, operation)
}

/*
  Rules are logical functions that used for list access, and may return a boolean (meaning
  all or no items are available) or a set of filters that limit the available items.
*/
module.exports = {
    canReadB2CAppAccessRightSets,
    canManageB2CAppAccessRightSets,
}
