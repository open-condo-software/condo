/**
 * Generated by `createschema miniapp.B2BAppPosIntegrationConfig 'paymentsAlertPageUrl:Text;'`
 */

const { userIsAdmin } = require('@open-condo/keystone/access')
const { historical, versioned, uuided, tracked, softDeleted, dvAndSender, analytical } = require('@open-condo/keystone/plugins')
const { GQLListSchema } = require('@open-condo/keystone/schema')

const access = require('@condo/domains/miniapp/access/B2CAppIntercomConfig')


const B2CAppIntercomConfig = new GQLListSchema('B2CAppIntercomConfig', {
    schemaDoc: 'B2CAppIntercomConfig',
    fields: {

        sendDTMFUrl: {
            schemaDoc: 'Url to send dtmf code for intercom. CallId and meta information are sent.',
            type: 'Url',
            sensitive: true,
            isRequired: false,
        },

        accessToken: {
            schemaDoc: 'Bearer token to include in requests.',
            type: 'EncryptedText',
            sensitive: true,
            isRequired: false,
            access: { read: userIsAdmin },
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), dvAndSender(), historical(), analytical()],
    access: {
        read: access.canReadB2CAppIntercomConfigs,
        create: access.canManageB2CAppIntercomConfigs,
        update: access.canManageB2CAppIntercomConfigs,
        delete: false,
        auth: true,
    },
})

module.exports = {
    B2CAppIntercomConfig,
}
