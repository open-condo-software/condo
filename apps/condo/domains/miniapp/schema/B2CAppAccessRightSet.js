/**
 * Generated by `createschema miniapp.B2CAppAccessRightSet 'app:Relationship:B2CApp:CASCADE'`
 */

const { historical, versioned, uuided, tracked, softDeleted, dvAndSender, analytical } = require('@open-condo/keystone/plugins')
const { GQLListSchema } = require('@open-condo/keystone/schema')

const access = require('@condo/domains/miniapp/access/B2CAppAccessRightSet')
const { generatePermissionFields } = require('@condo/domains/miniapp/utils/b2bAppServiceUserAccess/schema.utils')
const { B2C_APP_SERVICE_USER_ACCESS_AVAILABLE_SCHEMAS } = require('@condo/domains/miniapp/utils/b2cAppServiceUserAccess/config')


const B2CAppAccessRightSet = new GQLListSchema('B2CAppAccessRightSet', {
    schemaDoc: 'A set of access rights for a service user integrated with your B2CApp' +
        ' (exist B2CAppAccessRight with links to your service user and your B2CApp).' +
        '\nThis set of access right will be used to check your service user access to schemas and services that are linked to' +
        'your B2CApp\'s entities "B2CAppProperty" schema by "addressKey"',
    fields: {

        app: {
            schemaDoc: 'Link to B2CApp',
            type: 'Relationship',
            ref: 'B2CApp',
            isRequired: true,
            knexOptions: { isNotNullable: true }, // Required relationship only!
            kmigratorOptions: { null: false, on_delete: 'models.CASCADE' },
        },

        ...generatePermissionFields({ config: B2C_APP_SERVICE_USER_ACCESS_AVAILABLE_SCHEMAS }),
    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), dvAndSender(), historical(), analytical()],
    access: {
        read: access.canReadB2CAppAccessRightSets,
        create: access.canManageB2CAppAccessRightSets,
        update: access.canManageB2CAppAccessRightSets,
        delete: false,
        auth: true,
    },
})

module.exports = {
    B2CAppAccessRightSet,
}
