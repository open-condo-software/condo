/**
 * Generated by `createschema miniapp.B2BAppBillingEmbedConfig 'tabUrl?:Url'`
 */

const { GQLListSchema } = require('@open-condo/keystone/schema')
const { historical, versioned, uuided, tracked, softDeleted, dvAndSender, analytical } = require('@open-condo/keystone/plugins')
const access = require('@condo/domains/miniapp/access/B2BAppBillingEmbedConfig')


const B2BAppBillingEmbedConfig = new GQLListSchema('B2BAppBillingEmbedConfig', {
    schemaDoc: 'Configuration for embedding a B2BApp into the billing page. If specified on a B2BApp, the app will be available as an embeddable tab on the /billing page',
    fields: {

        tabUrl: {
            schemaDoc: 'URL of the mini-app page to open in an iframe tab on the /billing page',
            type: 'Url',
            sensitive: true,
            isRequired: false,
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), dvAndSender(), historical(), analytical()],
    access: {
        read: access.canReadB2BAppBillingEmbedConfigs,
        create: access.canManageB2BAppBillingEmbedConfigs,
        update: access.canManageB2BAppBillingEmbedConfigs,
        delete: false,
        auth: true,
    },
})

module.exports = {
    B2BAppBillingEmbedConfig,
}
