/**
 * Generated by `createschema miniapp.B2BAppBillingEmbeddingConfig 'tabUrl?:Url'`
 */

const { historical, versioned, uuided, tracked, softDeleted, dvAndSender, analytical } = require('@open-condo/keystone/plugins')
const { GQLListSchema } = require('@open-condo/keystone/schema')

const access = require('@condo/domains/miniapp/access/B2BAppBillingEmbeddingConfig')


const B2BAppBillingEmbeddingConfig = new GQLListSchema('B2BAppBillingEmbeddingConfig', {
    schemaDoc: 'Configuration for embedding a B2BApp into the billing page. If specified on a B2BApp, the app will be available as an embeddable tab on the /billing page',
    fields: {

        tabUrl: {
            schemaDoc: 'URL of the mini-app page to open in an iframe tab on the /billing page',
            type: 'Url',
            sensitive: true,
            isRequired: false,
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), dvAndSender(), historical(), analytical()],
    access: {
        read: access.canReadB2BAppBillingEmbeddingConfigs,
        create: access.canManageB2BAppBillingEmbeddingConfigs,
        update: access.canManageB2BAppBillingEmbeddingConfigs,
        delete: false,
        auth: true,
    },
})

module.exports = {
    B2BAppBillingEmbeddingConfig,
}
