/**
 * Generated by `createschema miniapp.B2CAppMessageSettings 'app?:Relationship:B2CApp:CASCADE; blockReason:Text; blockedNotificationTypes:Text; throttlingTime:Integer;'`
 */

const { historical, versioned, uuided, tracked, softDeleted, dvAndSender } = require('@open-condo/keystone/plugins')
const { GQLListSchema } = require('@open-condo/keystone/schema')

const access = require('@condo/domains/miniapp/access/B2CAppMessageSetting')
const { getMessageTypeField } = require('@condo/domains/notification/schema/fields/MessageType')


const B2CAppMessageSetting = new GQLListSchema('B2CAppMessageSetting', {
    schemaDoc: 'Configuration of sending push notifications for specific miniapps.' +
        'Used to block certain types of push notifications and control throttling of notifications of a certain miniapp',
    fields: {

        app: {
            schemaDoc: 'Link to B2CApp to which this config applies',
            type: 'Relationship',
            ref: 'B2CApp',
            isRequired: true,
            kmigratorOptions: { null: true, on_delete: 'models.CASCADE' },
        },

        blockReason: {
            schemaDoc: 'Reason for blocking messages with types from type',
            type: 'Text',
        },

        isBlacklisted: {
            schemaDoc: 'Is message type in black list for this B2CApp',
            type: 'Checkbox',
            isRequired: true,
            defaultValue: false,
        },

        type: getMessageTypeField({
            isRequired: true,
            schemaDoc: 'The type of message we want to configure',
        }),

        notificationWindowSize: {
            schemaDoc: 'Time window for sending push notifications in sec',
            type: 'Integer',
        },

        numberOfNotificationInWindow: {
            schemaDoc: 'The number of notifications that can be sent in one time window in sec',
            type: 'Integer',
        },

    },
    plugins: [uuided(), versioned(), tracked(), softDeleted(), dvAndSender(), historical()],
    access: {
        read: access.canReadB2CAppMessageSetting,
        create: access.canManageB2CAppMessageSetting,
        update: access.canManageB2CAppMessageSetting,
        delete: false,
        auth: true,
    },
    kmigratorOptions: {
        constraints: [
            {
                type: 'models.UniqueConstraint',
                fields: ['app', 'type'],
                condition: 'Q(deletedAt__isnull=True)',
                name: 'b2c_app_message_setting_unique_app_and_type',
            },
        ],
    },
})

module.exports = {
    B2CAppMessageSetting,
}
