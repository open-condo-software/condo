/**
 * Generated by `createschema miniapp.B2BAppAccessRight 'user:Relationship:User:PROTECT;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { B2BAppAccessRight as B2BAppAccessRightGQL } from '@condo/domains/miniapp/gql'
import { B2BAppAccessRight, B2BAppAccessRightUpdateInput, QueryAllB2BAppAccessRightsArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'user', 'app']
const RELATIONS = ['user', 'app']

export interface IB2BAppAccessRightUIState extends B2BAppAccessRight {
    id: string
}

function convertToUIState (item: B2BAppAccessRight): IB2BAppAccessRightUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IB2BAppAccessRightUIState
}

export interface IB2BAppAccessRightFormState {
    id?: undefined
}

function convertToUIFormState (state: IB2BAppAccessRightUIState): IB2BAppAccessRightFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IB2BAppAccessRightFormState
}

function convertToGQLInput (state: IB2BAppAccessRightFormState): B2BAppAccessRightUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<B2BAppAccessRight, B2BAppAccessRightUpdateInput, IB2BAppAccessRightFormState, IB2BAppAccessRightUIState, QueryAllB2BAppAccessRightsArgs>(B2BAppAccessRightGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
