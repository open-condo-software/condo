/**
 * Generated by `createschema miniapp.B2CApp 'name:Text;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { B2CApp as B2CAppGQL } from '@condo/domains/miniapp/gql'
import { B2CApp, B2CAppUpdateInput, QueryAllB2CAppsArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'name', 'isHidden', 'colorSchema']
const RELATIONS = []

export interface IB2CAppUIState extends B2CApp {
    id: string
}

function convertToUIState (item: B2CApp): IB2CAppUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IB2CAppUIState
}

export interface IB2CAppFormState {
    id?: undefined
}

function convertToUIFormState (state: IB2CAppUIState): IB2CAppFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IB2CAppFormState
}

function convertToGQLInput (state: IB2CAppFormState): B2CAppUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<B2CApp, B2CAppUpdateInput, IB2CAppFormState, IB2CAppUIState, QueryAllB2CAppsArgs>(B2CAppGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
