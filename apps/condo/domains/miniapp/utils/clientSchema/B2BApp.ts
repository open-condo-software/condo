/**
 * Generated by `createschema miniapp.B2BApp 'name:Text;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'
import { generateNewReactHooks } from '@condo/domains/common/utils/codegeneration/new.generate.hooks'

import { B2BApp as B2BAppGQL } from '@condo/domains/miniapp/gql'
import { B2BApp, B2BAppCreateInput, B2BAppUpdateInput, QueryAllB2BAppsArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'name', 'logo', 'shortDescription', 'about', 'developer', 'partnerUrl', 'instruction', 'connectedMessage', 'appUrl', 'category', 'setupButtonMessage']
const RELATIONS = []

export interface IB2BAppUIState extends B2BApp {
    id: string
}

function convertToUIState (item: B2BApp): IB2BAppUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IB2BAppUIState
}

export interface IB2BAppFormState {
    id?: undefined
}

function convertToUIFormState (state: IB2BAppUIState): IB2BAppFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IB2BAppFormState
}

function convertToGQLInput (state: IB2BAppFormState): B2BAppUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<B2BApp, B2BAppUpdateInput, IB2BAppFormState, IB2BAppUIState, QueryAllB2BAppsArgs>(B2BAppGQL, { convertToGQLInput, convertToUIState })
const {
    useNewObject,
    useNewObjects,
    useNewCreate,
    useNewUpdate,
    useNewSoftDelete,
} = generateNewReactHooks<B2BApp, B2BAppCreateInput, B2BAppUpdateInput, QueryAllB2BAppsArgs>(B2BAppGQL)

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
    useNewObject,
    useNewObjects,
    useNewCreate,
    useNewUpdate,
    useNewSoftDelete,
}
