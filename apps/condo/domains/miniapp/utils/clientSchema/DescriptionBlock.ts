/**
 * Generated by `createschema miniapp.DescriptionBlock 'description:Text; image:File;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { DescriptionBlock as DescriptionBlockGQL } from '@condo/domains/miniapp/gql'
import { DescriptionBlock, DescriptionBlockUpdateInput, QueryAllDescriptionBlocksArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'title', 'description', 'image']
const RELATIONS = []

export interface IDescriptionBlockUIState extends DescriptionBlock {
    id: string
}

function convertToUIState (item: DescriptionBlock): IDescriptionBlockUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IDescriptionBlockUIState
}

export interface IDescriptionBlockFormState {
    id?: undefined
}

function convertToUIFormState (state: IDescriptionBlockUIState): IDescriptionBlockFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IDescriptionBlockFormState
}

function convertToGQLInput (state: IDescriptionBlockFormState): DescriptionBlockUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<DescriptionBlock, DescriptionBlockUpdateInput, IDescriptionBlockFormState, IDescriptionBlockUIState, QueryAllDescriptionBlocksArgs>(DescriptionBlockGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
