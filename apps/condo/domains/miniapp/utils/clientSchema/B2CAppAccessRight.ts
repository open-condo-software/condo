/**
 * Generated by `createschema miniapp.B2CAppAccessRight 'user:Relationship:User:PROTECT; app:Relationship:B2CApp:PROTECT;'`
 */

import { pick, get } from 'lodash'

import { getClientSideSenderInfo } from '@condo/domains/common/utils/userid.utils'
import { generateReactHooks } from '@condo/domains/common/utils/codegeneration/generate.hooks'

import { B2CAppAccessRight as B2CAppAccessRightGQL } from '@condo/domains/miniapp/gql'
import { B2CAppAccessRight, B2CAppAccessRightUpdateInput, QueryAllB2CAppAccessRightsArgs } from '../../../../schema'

const FIELDS = ['id', 'deletedAt', 'createdAt', 'updatedAt', 'createdBy', 'updatedBy', 'user', 'app']
const RELATIONS = ['user', 'app']

export interface IB2CAppAccessRightUIState extends B2CAppAccessRight {
    id: string
}

function convertToUIState (item: B2CAppAccessRight): IB2CAppAccessRightUIState {
    if (item.dv !== 1) throw new Error('unsupported item.dv')
    return pick(item, FIELDS) as IB2CAppAccessRightUIState
}

export interface IB2CAppAccessRightFormState {
    id?: undefined
}

function convertToUIFormState (state: IB2CAppAccessRightUIState): IB2CAppAccessRightFormState | undefined {
    if (!state) return
    const result = {}
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? attrId || state[attr] : state[attr]
    }
    return result as IB2CAppAccessRightFormState
}

function convertToGQLInput (state: IB2CAppAccessRightFormState): B2CAppAccessRightUpdateInput {
    const sender = getClientSideSenderInfo()
    const result = { dv: 1, sender }
    for (const attr of Object.keys(state)) {
        const attrId = get(state[attr], 'id')
        result[attr] = (RELATIONS.includes(attr) && state[attr]) ? { connect: { id: (attrId || state[attr]) } } : state[attr]
    }
    return result
}

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
} = generateReactHooks<B2CAppAccessRight, B2CAppAccessRightUpdateInput, IB2CAppAccessRightFormState, IB2CAppAccessRightUIState, QueryAllB2CAppAccessRightsArgs>(B2CAppAccessRightGQL, { convertToGQLInput, convertToUIState })

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useDelete,
    convertToUIFormState,
}
