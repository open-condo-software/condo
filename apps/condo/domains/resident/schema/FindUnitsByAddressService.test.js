/**
 * Generated by `createservice resident.FindUnitsByAddressService --type queries`
 */

const { makeLoggedInAdminClient, makeClient } = require('@open-condo/keystone/test.utils')
const { expectToThrowAccessDeniedErrorToObj, expectToThrowAuthenticationErrorToObjects } = require('@open-condo/keystone/test.utils')

const { findUnitsByAddressByTestClient } = require('@condo/domains/resident/utils/testSchema')
 
describe('FindUnitsByAddressService', () => {
    test('admin: execute', async () => {
        const admin = await makeLoggedInAdminClient()
        const payload = {}
        const [data, attrs] = await findUnitsByAddressByTestClient(admin, payload)

        throw new Error('Not implemented yet')
    })
})