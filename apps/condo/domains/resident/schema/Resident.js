/**
 * Generated by `createschema resident.Resident 'user:Relationship:User:CASCADE; organization:Relationship:Organization:PROTECT; property:Relationship:Property:PROTECT; billingAccount?:Relationship:BillingAccount:SET_NULL; unitName:Text;'`
 */
const get = require('lodash/get')
const has = require('lodash/has')
const isNull = require('lodash/isNull')
const omit = require('lodash/omit')
const pick = require('lodash/pick')

const { historical, versioned, uuided, tracked, softDeleted, dvAndSender, analytical } = require('@open-condo/keystone/plugins')
const { addressService } = require('@open-condo/keystone/plugins/addressService')
const { GQLListSchema, find, getById, getByCondition } = require('@open-condo/keystone/schema')

const { DEFAULT_ACQUIRING_INTEGRATION_NAME } = require('@condo/domains/acquiring/constants/integration')
const { removeOrphansRecurrentPaymentContexts } = require('@condo/domains/acquiring/tasks/removeRecurrentPayments')
const { AcquiringIntegrationContext } = require('@condo/domains/acquiring/utils/serverSchema')
const { DEFAULT_BILLING_INTEGRATION_NAME } = require('@condo/domains/billing/constants/constants')
const { BillingIntegrationOrganizationContext } = require('@condo/domains/billing/utils/serverSchema')
const { CLIENT_PHONE_FIELD, UNIT_TYPE_FIELD } = require('@condo/domains/common/schema/fields')
const { getUnitTypeFieldResolveInput } = require('@condo/domains/common/utils/serverSchema/resolveHelpers')
const { Meter } = require('@condo/domains/meter/utils/serverSchema')
const { addOrganizationFieldPlugin } = require(
    '@condo/domains/organization/schema/plugins/addOrganizationFieldPlugin')
const { Property } = require('@condo/domains/property/utils/serverSchema')
const { getAddressUpToBuildingFrom } = require('@condo/domains/property/utils/serverSchema/helpers')
const access = require('@condo/domains/resident/access/Resident')
const { PAYMENT_CATEGORIES_META } = require('@condo/domains/resident/constants')
const {
    RESIDENT_PROPERTY_FIELDS,
    ORGANIZATION_FEATURES_FIELDS,
    PAYMENT_CATEGORIES_FIELDS,
} = require('@condo/domains/resident/gql')
const { resetUserResidentCache } = require('@condo/domains/resident/utils/accessSchema')
const { getActiveOccupancy, getActiveOccupancyByGraphQL } = require('@condo/domains/resident/utils/serverSchema')
const { RESIDENT: RESIDENT_USER_TYPE  } = require('@condo/domains/user/constants/common')

const { RESIDENT_ORGANIZATION_FIELD } = require('./fields')

const { manageResidentToTicketClientConnections } = require('../tasks')

const CURRENT_OCCUPANCY_FIELDS = 'id status startDate expectedEndDate actualEndDate monthlyRate billingFrequency rentalUnit { id name unitType } property { id name address addressKey }'
const RESIDENT_SERVICE_CONSUMER_IDS_TO_CLEANUP = '_residentServiceConsumerIdsToCleanup'
const residentServiceConsumerIdsToCleanup = new Map()

function normalizeString (value) {
    return String(value || '').trim().toLowerCase()
}

function getResidentAddressUpToBuilding (resident) {
    return getAddressUpToBuildingFrom(get(resident, 'addressMeta')) || get(resident, 'address')
}

const addressFieldHooks = {
    resolveInput: async ({ resolvedData: { address, addressMeta } }) => {
        const newAddress = getAddressUpToBuildingFrom(addressMeta)
        return newAddress || address
    },
}

/**
 * Obtains a resident's associated managing company contact
 * @param {Record<string, unknown>} item
 * @returns {Promise<Record<string, unknown>|null>}
 */
async function getManagingCompanyContactFromItem (item) {
    const propertyId = get(item, 'property')
    const unitName = get(item, 'unitName')
    const unitType = get(item, 'unitType')
    const userId = get(item, 'user')

    if (!propertyId || !unitName || !unitType || !userId) {
        return null
    }

    const user = await getByCondition('User', {
        id: userId,
        deletedAt: null,
        type: RESIDENT_USER_TYPE,
    })

    if (!user || !user.phone) {
        return null
    }

    return await getByCondition('Contact', {
        property: { id: propertyId },
        unitName,
        unitType,
        phone: user.phone,
        deletedAt: null,
    })
}

const Resident = new GQLListSchema('Resident', {
    schemaDoc: 'Person, that resides in a specified property and unit',
    fields: {
        user: {
            schemaDoc: 'Mobile user account',
            type: 'Relationship',
            ref: 'User',
            isRequired: true,
            knexOptions: { isNotNullable: true }, // Relationship only!
            kmigratorOptions: { null: false, on_delete: 'models.CASCADE' },
        },

        // The reason for this field is to avoid adding check for resident user into global Organization read access.
        // This field have specific use case for mobile client.
        residentOrganization: {
            ...RESIDENT_ORGANIZATION_FIELD,
        },

        property: {
            schemaDoc: 'Property, in which this person resides. Can be missing, when a resident has been registered, but there is no Property in our system yet',
            type: 'Relationship',
            ref: 'Property',
            isRequired: false,
            knexOptions: { isNotNullable: false }, // Required relationship only!
            kmigratorOptions: { null: true, on_delete: 'models.SET_NULL' },
            hooks: {
                validateInput: async ({ context, resolvedData, operation, addFieldValidationError }) => {
                    if (operation === 'update') return
                    const propertyId = resolvedData.property
                    if (!propertyId) return
                    const [property] = await Property.getAll(context, { id: propertyId }, 'id address addressKey')
                    const residentAddress = getAddressUpToBuildingFrom(resolvedData.addressMeta)
                    if (property.address.toLowerCase() !== residentAddress.toLowerCase()) {
                        // but wait... if these addresses were updated by address service, the addressKey will be the same
                        let areAddressesEqual = false

                        if (!!property.addressKey && !!resolvedData.addressKey && property.addressKey === resolvedData.addressKey) {
                            areAddressesEqual = true
                        }

                        if (!areAddressesEqual) {
                            return addFieldValidationError('Cannot connect property, because its address differs from address of resident')
                        }
                    }
                },
            },
        },

        // The reason for this field is to avoid adding check for resident user into global Property read access.
        // This field have specific use case for mobile client.
        residentProperty: {
            schemaDoc: 'Property data, that is returned for current resident in mobile client',
            type: 'Virtual',
            extendGraphQLTypes: ['type ResidentProperty { id: ID!, name: String, address: String!, addressKey: String }'],
            graphQLReturnType: 'ResidentProperty',
            graphQLReturnFragment: `{ ${RESIDENT_PROPERTY_FIELDS} }`,
            resolver: async (item) => {
                if (item.property) {
                    const property = await getById('Property', item.property)
                    return pick(property, ['id', 'name', 'address', 'addressKey'])
                } else {
                    return null
                }
            },
            access: true,
        },

        organizationFeatures: {
            schemaDoc: 'Contains features that are enabled for user organization',
            type: 'Virtual',
            extendGraphQLTypes: ['type OrganizationFeatures { hasBillingData: Boolean!, hasMeters: Boolean! }'],
            graphQLReturnType: 'OrganizationFeatures',
            graphQLReturnFragment: `{ ${ORGANIZATION_FEATURES_FIELDS} }`,
            resolver: async (item, _, context) => {
                if (item.organization) {
                    const organizationId = get(item, 'organization', null)
                    const billingContextsCount = await BillingIntegrationOrganizationContext.count(context, {
                        organization: { id: organizationId },
                        lastReport_not: null,
                        deletedAt: null,
                    })

                    const propertyId = get(item, 'property', null)
                    const unitName = get(item, 'unitName', null)
                    const metersCount = await Meter.count(context, {
                        organization: { id: organizationId },
                        property: { id: propertyId },
                        unitName: unitName,
                        deletedAt: null,
                    })

                    return {
                        hasBillingData: billingContextsCount > 0,
                        hasMeters: metersCount > 0,
                    }
                } else {
                    return null
                }
            },
            access: true,
        },

        // TODO(pahaz): we should remove this field. Because you don't need it anymore. You can get billing/acquiring from ?!
        paymentCategories: {
            schemaDoc: 'Contains billing and acquiring integration names that are enabled for this resident per category',
            type: 'Virtual',
            extendGraphQLTypes: ['type PaymentCategory { id: String!, categoryName: String!, billingName: String! acquiringName: String! }'],
            graphQLReturnType: '[PaymentCategory]',
            graphQLReturnFragment: `{ ${PAYMENT_CATEGORIES_FIELDS} }`,
            resolver: async (item, _, context) => {
                return PAYMENT_CATEGORIES_META
                    .filter(x => x.active)
                    .map(async category => {
                        let billingName = DEFAULT_BILLING_INTEGRATION_NAME
                        let acquiringName = DEFAULT_ACQUIRING_INTEGRATION_NAME

                        if (category.canGetBillingFromOrganization && item.organization) {
                            const [billingCtx] = await BillingIntegrationOrganizationContext.getAll(
                                context, { organization: { id: item.organization, deletedAt: null }, deletedAt: null },
                                'id integration { name }'
                            )
                            billingName = get(billingCtx, ['integration', 'name'], DEFAULT_BILLING_INTEGRATION_NAME)
                        }

                        if (category.canGetAcquiringFromOrganization && item.organization) {
                            const [acquiringCtx] = await AcquiringIntegrationContext.getAll(
                                context, { organization: { id: item.organization, deletedAt: null }, deletedAt: null },
                                'integration { name }'
                            )
                            acquiringName = get(acquiringCtx, ['integration', 'name'], DEFAULT_ACQUIRING_INTEGRATION_NAME)
                        }

                        return {
                            id: category.id,
                            categoryName: category.name,
                            billingName: billingName,
                            acquiringName: acquiringName,
                        }
                    })
            },
        },

        unitName: {
            schemaDoc: '@deprecated Use Occupancy.rentalUnit instead. Legacy unit of the property, kept only for migration compatibility.',
            type: 'Text',
            isRequired: false,
        },

        unitType: {
            ...UNIT_TYPE_FIELD,
            schemaDoc: '@deprecated Use Occupancy.rentalUnit.unitType instead.',
            hooks: {
                resolveInput: getUnitTypeFieldResolveInput(),
            },
        },
        occupancies: {
            schemaDoc: 'Occupancy agreements for this tenant',
            type: 'Relationship',
            ref: 'Occupancy.tenant',
            many: true,
            access: { create: false, update: false },
        },
        currentOccupancy: {
            schemaDoc: 'Current active occupancy for this tenant',
            type: 'Virtual',
            graphQLReturnType: 'Occupancy',
            graphQLReturnFragment: `{ ${CURRENT_OCCUPANCY_FIELDS} }`,
            resolver: async (item, _, context) => {
                return await getActiveOccupancyByGraphQL(context, { tenantId: item.id }, CURRENT_OCCUPANCY_FIELDS)
            },
        },
        ghanaCardNumber: {
            schemaDoc: 'Ghana Card number for tenant identity checks',
            type: 'Text',
            isRequired: false,
        },
        emergencyContactName: {
            schemaDoc: 'Tenant emergency contact name',
            type: 'Text',
            isRequired: false,
        },
        emergencyContactPhone: {
            ...CLIENT_PHONE_FIELD,
            schemaDoc: 'Tenant emergency contact phone',
        },
        institutionName: {
            schemaDoc: 'Tenant institution name for student accommodation',
            type: 'Text',
            isRequired: false,
        },
        studentIdNumber: {
            schemaDoc: 'Tenant student ID number',
            type: 'Text',
            isRequired: false,
        },
        programme: {
            schemaDoc: 'Tenant academic programme',
            type: 'Text',
            isRequired: false,
        },
        level: {
            schemaDoc: 'Tenant academic level',
            type: 'Text',
            isRequired: false,
        },
        isVerifiedByManagingCompany: {
            schema: 'Verification status of the relevant contact of the managing company to which the resident is linked',
            type: 'Virtual',
            graphQLReturnType: 'Boolean',
            resolver: async (item) => {
                const contact = await getManagingCompanyContactFromItem(item)

                return Boolean(contact && contact.isVerified)
            },
        },
        managingCompanyOwnershipPercentage: {
            schema: 'Contact ownership percentage',
            type: 'Virtual',
            graphQLReturnType: 'String',
            resolver: async (item) => {
                const contact = await getManagingCompanyContactFromItem(item)
                return contact ? contact.ownershipPercentage : null
            },
        },
        managingCompanyCommunityFee: {
            schema: 'Contact community fee',
            type: 'Virtual',
            graphQLReturnType: 'String',
            resolver: async (item) => {
                const contact = await getManagingCompanyContactFromItem(item)
                return contact ? contact.communityFee : null
            },
        },
        managingCompanyContactRole: {
            schema: 'Role of the contact corresponding to this resident in the managing company',
            type: 'Virtual',
            extendGraphQLTypes: ['type ResidentContactRole { id: ID!, name: String!, organization: ID }'],
            graphQLReturnType: 'ResidentContactRole',
            resolver: async (item) => {
                const contact = await getManagingCompanyContactFromItem(item)
                if (!contact || !contact.role) {
                    return null
                }

                return await getByCondition('ContactRole', { id: contact.role, deletedAt: null })
            },
        },
    },
    plugins: [
        addOrganizationFieldPlugin({ fromField: 'property' }),
        uuided(),
        addressService({
            fieldsHooks: { address: addressFieldHooks },
            resolveAddressFields: ({ addressFields, operation }) => {
                return operation === 'update'
                    ? omit(addressFields, ['address', 'addressMeta'])
                    : addressFields
            },
        }),
        versioned(),
        tracked(),
        softDeleted(),
        dvAndSender(),
        historical(),
        analytical(),
    ],
    hooks: {
        beforeChange: async ({ operation, existingItem, resolvedData, context }) => {
            if (operation !== 'update' || !get(resolvedData, 'deletedAt') || !existingItem) return

            const serviceConsumers = await find('ServiceConsumer', {
                resident: { id: existingItem.id },
                deletedAt: null,
            })

            const serviceConsumerIds = serviceConsumers.map(({ id }) => id)
            context[RESIDENT_SERVICE_CONSUMER_IDS_TO_CLEANUP] = serviceConsumerIds
            residentServiceConsumerIdsToCleanup.set(existingItem.id, serviceConsumerIds)
        },
        validateInput: async ({ resolvedData, operation, addValidationError, context, originalInput }) => {
            const { address, addressMeta, unitName, user: userId } = resolvedData
            if (operation === 'create') {
                if (userId && address && unitName) {
                    const sameUserResidents = await find('Resident', {
                        user: { id: userId },
                        deletedAt: null,
                    })
                    const residentAddress = getAddressUpToBuildingFrom(addressMeta) || address
                    const hasDuplicateResident = sameUserResidents.some(resident => {
                        return normalizeString(getResidentAddressUpToBuilding(resident)) === normalizeString(residentAddress)
                            && normalizeString(resident.unitName) === normalizeString(unitName)
                    })

                    if (hasDuplicateResident) {
                        return addValidationError('Cannot create resident, because another resident with the same provided "address" and "unitName" already exists for current user')
                    }
                }

                const occupancy = await getActiveOccupancy({ userId })
                if (occupancy) {
                    return addValidationError('Cannot create resident, because another active occupancy already exists for current user')
                }
            } else if (operation === 'update') {
                if (
                    has(originalInput, 'address')
                    || has(originalInput, 'addressMeta')
                    || has(originalInput, 'unitName')
                    || has(originalInput, 'property')
                ) {
                    return addValidationError('Changing of address, addressMeta, unitName or property is not allowed for already existing Resident')
                }
            }
        },
        afterChange: async ({ operation, updatedItem, originalInput, context }) => {
            const deletedAt = get(updatedItem, 'deletedAt', null)
            const userId = get(updatedItem, 'user', null)
            const propertyId = get(updatedItem, 'property', null)
            const unitName = get(updatedItem, 'unitName', null)
            const unitType = get(updatedItem, 'unitType', null)
            const sender = get(originalInput, 'sender', null)
            const dv = get(originalInput, 'dv', null)

            const changedPropertyId = get(originalInput, ['property', 'connect', 'id'], null)
            const changedUnitName = get(originalInput, 'unitName', null)
            const changedUnitType = get(originalInput, 'unitType', null)
            await resetUserResidentCache(userId)

            if (!isNull(changedPropertyId) || !isNull(changedUnitName) || !isNull(changedUnitType)) {
                await manageResidentToTicketClientConnections.delay(propertyId, unitType, unitName, userId, dv, sender)
            }

            // handle soft delete
            // in order to soft delete recurrent payment contexts
            if (operation === 'update' && deletedAt) {
                const serviceConsumerIds = get(context, RESIDENT_SERVICE_CONSUMER_IDS_TO_CLEANUP, residentServiceConsumerIdsToCleanup.get(updatedItem.id) || [])
                residentServiceConsumerIdsToCleanup.delete(updatedItem.id)

                for (const serviceConsumerId of serviceConsumerIds) {
                    await removeOrphansRecurrentPaymentContexts({
                        serviceConsumerId,
                        dv,
                        sender,
                    })
                }

                await removeOrphansRecurrentPaymentContexts({
                    residentId: updatedItem.id,
                    dv,
                    sender,
                })
            }
        },
    },
    access: {
        read: access.canReadResidents,
        create: access.canManageResidents,
        update: access.canManageResidents,
        delete: false,
        auth: true,
    },
})

module.exports = {
    Resident,
}
