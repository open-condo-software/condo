/**
 * Generated by `createservice user.ChangeTwoFactorAuthenticationService --type mutations`
 */
const { throwAuthenticationError } = require('@open-condo/keystone/apolloErrorFormatter')


/**
 * 2FA can only be changed by:
 * 1. Non-deleted authorized user.
 */
async function canChangeTwoFactorAuthentication ({ authentication: { item: user } }) {
    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false

    return true
}

/*
  Rules are logical functions that used for list access, and may return a boolean (meaning
  all or no items are available) or a set of filters that limit the available items.
*/
module.exports = {
    canChangeTwoFactorAuthentication,
}