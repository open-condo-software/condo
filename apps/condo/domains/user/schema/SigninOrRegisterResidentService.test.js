/**
 * Generated by `createservice user.SigninOrRegisterResidentService`
 */

const { makeLoggedInAdminClient, makeClient } = require('@core/keystone/test.utils')
const { expectToThrowAccessDeniedErrorToObj } = require('@condo/domains/common/utils/testSchema')

const { signinOrRegisterResidentByTestClient } = require('@condo/domains/user/utils/testSchema')

describe('SigninOrRegisterResidentService', () => {
    describe('User', () => {
        it('can not register with token', async () => {
            const client = await makeClient()
        })
    })
    describe('Anonymous', () => {
        it('can register with valid token', async () => {
            const client = await makeClient()
        })
        it('can signin with valid token if already existed', async () => {
            const client = await makeClient()
        })
        it('will get error if token is used', async () => {

        })
        it('will get error if token is not confirmed', async () => {

        })
        it('will get error if token is expired', async () => {

        })

    })
})
