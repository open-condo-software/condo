const { versioning } = require('./versioning')
const { historical } = require('./historical')
const { uuiding } = require('./uuiding')

module.exports = {
    versioning,
    historical,
    uuiding,
}
