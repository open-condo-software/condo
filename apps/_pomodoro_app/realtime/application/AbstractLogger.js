/**
 * Logger is the function that stores the data about the sessions and processes it
 */
class AbstractLogger {

    /**
     * Process single entry
     * @param entry
     */
    log(entry) {}
}

module.exports = AbstractLogger
