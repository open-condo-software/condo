/**
 * Generated by `createschema user.ConfirmPhoneAction 'code:Text; isVerified:Checkbox; expiresAt:DateTimeUtc'`
 */

const { GQLListSchema } = require('@open-condo/keystone/schema')

const { CONFIRM_PHONE_ACTION_MAX_ATTEMPTS } = require('@dev-portal-api/domains/user/constants')
const { getConfirmActionModel } = require('@dev-portal-api/domains/user/schema/fields/ConfirmAction')

const ConfirmPhoneAction = new GQLListSchema('ConfirmPhoneAction', getConfirmActionModel('phone', CONFIRM_PHONE_ACTION_MAX_ATTEMPTS))

module.exports = {
    ConfirmPhoneAction,
}
