/**
 * Generated by `createschema miniapp.B2BAppAccessRight 'app:Relationship:B2BApp:CASCADE; condoUserId:Text'`
 */

const { faker } = require('@faker-js/faker')

const {
    makeClient,
    expectToThrowAccessDeniedErrorToObj,
    expectToThrowAuthenticationErrorToObj,
} = require('@open-condo/keystone/test.utils')

const { normalizeEmail } = require('@app/condo/domains/common/utils/mail')
const {
    createTestB2BApp,
    B2BAppAccessRight,
    createTestB2BAppAccessRight,
    updateTestB2BAppAccessRight,
    registerAppUserServiceByTestClient,
} = require('@dev-portal-api/domains/miniapp/utils/testSchema')
const {
    makeLoggedInAdminClient,
    makeLoggedInSupportClient,
    makeRegisteredAndLoggedInUser,
    verifyEmailByTestClient,
} = require('@dev-portal-api/domains/user/utils/testSchema')


describe('B2BAppAccessRight', () => {
    let actors = {
        admin: undefined,
        support: undefined,
        user: undefined,
        anonymous: undefined,
    }

    let app
    let email
    let condoUserId
    let accessRight
    const accessDeniedRoles = ['admin', 'support', 'user']
    beforeAll(async () => {
        actors.admin = await makeLoggedInAdminClient()
        actors.support = await makeLoggedInSupportClient()
        actors.user = await makeRegisteredAndLoggedInUser()
        actors.anonymous = await makeClient();

        [app] = await createTestB2BApp(actors.user)
        email = faker.internet.email()
        const confirmAction = await verifyEmailByTestClient(actors.user, actors.admin, email)
        const [{ id }] = await registerAppUserServiceByTestClient(actors.user, app, confirmAction)
        condoUserId = id
        accessRight = await B2BAppAccessRight.getOne(actors.admin, { app: { id: app.id } })
    })
    describe('CRUD tests', () => {
        describe('Create', () => {
            describe('B2BAppAccessRight cannot be created manually', () => {
                test.each(accessDeniedRoles)('%p cannot create', async (role) => {
                    await expectToThrowAccessDeniedErrorToObj(async () => {
                        await createTestB2BAppAccessRight(actors[role], app, condoUserId)
                    })
                })
                test('Anonymous cannot create', async () => {
                    await expectToThrowAuthenticationErrorToObj(async () => {
                        await createTestB2BAppAccessRight(actors.anonymous, app, condoUserId)
                    })
                })
            })
        })
        describe('Update', () => {
            describe('B2BAppAccessRight cannot be updated manually', () => {
                test.each(accessDeniedRoles)('%p cannot update', async (role) => {
                    await expectToThrowAccessDeniedErrorToObj(async () => {
                        await updateTestB2BAppAccessRight(actors[role], accessRight.id, { condoUserId: faker.datatype.uuid() })
                    })
                })
                test('Anonymous cannot update', async () => {
                    await expectToThrowAuthenticationErrorToObj(async () => {
                        await updateTestB2BAppAccessRight(actors.anonymous, accessRight.id, { condoUserId: faker.datatype.uuid() })
                    })
                })
            })
        })
        describe('Read', () => {
            test('Admin can read any B2BAccessRight', async () => {
                const right = await B2BAppAccessRight.getOne(actors.admin, { app: { id: app.id } })
                expect(right).toBeDefined()
            })
            test('Support can read any B2BAccessRight', async () => {
                const right = await B2BAppAccessRight.getOne(actors.support, { app: { id: app.id } })
                expect(right).toBeDefined()
            })
            describe('User', () => {
                test('Can read B2BAccessRight linked to app he created', async () => {
                    const right = await B2BAppAccessRight.getOne(actors.user, { app: { id: app.id } })
                    expect(right).toBeDefined()
                    expect(right).toHaveProperty('condoUserEmail', normalizeEmail(email))
                })
                test('Cannot read other B2BAccessRights', async () => {
                    const anotherUser = await makeRegisteredAndLoggedInUser()
                    const right = await B2BAppAccessRight.getOne(anotherUser, { app: { id: app.id } })
                    expect(right).not.toBeDefined()
                })
            })
        })
        describe('Hard-delete', () => {
            describe('B2BAppAccessRight cannot be hard-deleted', () => {
                test.each(Object.keys(actors))('%p cannot hard-delete', async (role) => {
                    await expectToThrowAccessDeniedErrorToObj(async () => {
                        await B2BAppAccessRight.delete(actors[role], accessRight.id)
                    })
                })
            })
        })
    })
})
