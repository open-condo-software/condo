/**
 * Generated by `createservice miniapp.UpdateB2BAppContextService`
 */
const { throwAuthenticationError } = require('@open-condo/keystone/apolloErrorFormatter')

const { canExecuteB2BAppMutationAsOwner } = require('@dev-portal-api/domains/miniapp/utils/serverSchema/access')

async function canUpdateB2BAppContext (params) {
    const { authentication: { item: user } } = params

    if (!user) return throwAuthenticationError()
    if (user.deletedAt) return false
    if (user.isAdmin || user.isSupport) return true

    return await canExecuteB2BAppMutationAsOwner(params)
}

/*
  Rules are logical functions that used for list access, and may return a boolean (meaning
  all or no items are available) or a set of filters that limit the available items.
*/
module.exports = {
    canUpdateB2BAppContext,
}