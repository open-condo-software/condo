query allB2BAppAccessRights($appId: ID!, $environment: AppEnvironment!) {
    rights: allB2BAppAccessRights(where: { app: { id: $appId }, environment: $environment }, first: 1, skip: 0) {
        condoUserEmail
    }
}