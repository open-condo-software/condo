const DV_VERSION_MISMATCH = 'DV_VERSION_MISMATCH'

// Provided value does not match the specified format. For example, not matches regexp, string length requirement, etc.
const WRONG_FORMAT = 'WRONG_FORMAT'

module.exports = {
    DV_VERSION_MISMATCH,
    WRONG_FORMAT,
}
