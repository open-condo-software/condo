{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**", "build/**"]
    },
    "lint-translations":{
      "cache": false,
      "outputs": []
    },
    "prepare": {
      "dependsOn": ["^prepare"],
      "cache": false
    }
  }
}