/**
 * Generated by `{{ command }}`
 */

import {
    {{ name }},
    {{ name }}CreateInput,
    {{ name }}UpdateInput,
    QueryAll{{ pluralize.plural(name) }}Args,
} from '@app/{{app}}/schema'
import { generateReactHooks } from '@open-condo/codegen/generate.hooks'
import { {{ name }} as {{ name }}GQL } from '@{{app}}/domains/{{ domain }}/gql'

// TODO(codegen): write utils like convertToFormState and formValuesProcessor if needed, otherwise delete this TODO

const {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useSoftDelete,
} = generateReactHooks<{{ name }}, {{ name }}CreateInput, {{ name }}UpdateInput, QueryAll{{ pluralize.plural(name) }}Args>({{ name }}GQL)

export {
    useObject,
    useObjects,
    useCreate,
    useUpdate,
    useSoftDelete,
}
