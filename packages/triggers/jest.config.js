module.exports = {
    projects: [
        {
            displayName: '@open-condo/triggers',
            testEnvironment: 'node',
            testMatch: [`${__dirname}/*.spec.js`],
        },
    ],
}
