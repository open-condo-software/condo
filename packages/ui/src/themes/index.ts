import { Theme } from './types'
import { colors as defaultColors, gradients as defaultGradients } from './default'

export const defaultTheme: Theme = {
    colors: defaultColors,
    gradients: defaultGradients,
}