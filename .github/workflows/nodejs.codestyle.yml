name: TEST CODESTYLE (CURRENTLY DISABLED)

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x]

    steps:
      - uses: actions/checkout@v2
      - name: Check lint with prettier
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      #- run: yarn install --verbose --registry https://registry.npmjs.org
      #- run: yarn prettier --check .
      - name: Check packages.json
        run: yarn manypkg check
