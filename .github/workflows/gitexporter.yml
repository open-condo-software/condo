name: gitexporter
on:
  schedule:
    - cron:  '10 */1 * * *'
  push:
    branches:
      - 'gitexporter'

jobs:
  gitexporter:
    name: Gitexporter
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ssh-key: ${{ secrets.RUNNER_SSH_PRIVATE_KEY }}
      - name: Checkout source code
        uses: actions/checkout@v2
        with:
          repository: 'open-condo-software/condo'
          fetch-depth: 0
          submodules: recursive
          ssh-key: ${{ secrets.RUNNER_SSH_PRIVATE_KEY }}
          path: ignore.gitexporter.source
          ref: 'master'
      - name: Checkout target code
        uses: actions/checkout@v2
        with:
          repository: 'open-condo-software/open-condo-platform'
          fetch-depth: 0
          submodules: recursive
          ssh-key: ${{ secrets.RUNNER_SSH_PRIVATE_KEY }}
          path: ignore.gitexporter.target
          ref: 'master'
      - name: gitexporter.sh
        run: |
          ls -lah .github/workflows/
          bash .github/workflows/gitexporter.sh
