const PROJECT_ENV_PATH = '../../../.env'
const ENV_PATH = './.env'

const ENV_REQUIRED_FIELDS = [
    'SERVER_URL',
    'USER_NAME', 'USER_CITY', 'USER_COUNTRY',
    'USER_PHONE', 'USER_EMAIL', 'USER_PASSWORD', 'USER_ADDRESS',
    'ADMIN_PHONE', 'ADMIN_EMAIL', 'ADMIN_PASSWORD',
]
const PROJECT_ENV_REQUIRED_FIELDS = ['ADDRESS_SUGGESTIONS_CONFIG']

const DEFAULT_VARIABLES = { data: { where: {} } }

module.exports = {
    PROJECT_ENV_REQUIRED_FIELDS,
    ENV_REQUIRED_FIELDS,
    DEFAULT_VARIABLES,
    PROJECT_ENV_PATH,
    ENV_PATH,
}